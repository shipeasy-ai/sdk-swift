//
// ConfigsAPI.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

open class ConfigsAPI {

    /**
     Create a dynamic config
     
     - parameter createConfigRequest: (body)  
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - returns: CreateConfigResponse
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func createConfig(createConfigRequest: CreateConfigRequest, xProjectId: String? = nil) async throws -> CreateConfigResponse {
        return try await createConfigWithRequestBuilder(createConfigRequest: createConfigRequest, xProjectId: xProjectId).execute().body
    }

    /**
     Create a dynamic config
     - POST /api/admin/configs
     - Creates a new config with the given `schema`. The initial `value` (or an empty object) is published as version 1 on **every** env.  Returns `409` if `name` already exists in the project, `400` if `value` doesn't validate against `schema`.  **Use cases**  - **Minimal create** — `name` + `schema`. Initial value defaults to `{}`. - **Seeded create** — supply a flat `value` to publish the same object on every env. - **Per-env seed** — supply a `{ env: value }` map for different per-env starting values.
     - Bearer Token:
       - type: http
       - name: bearerSdkKey
     - parameter createConfigRequest: (body)  
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - returns: RequestBuilder<CreateConfigResponse> 
     */
    open class func createConfigWithRequestBuilder(createConfigRequest: CreateConfigRequest, xProjectId: String? = nil) -> RequestBuilder<CreateConfigResponse> {
        let localVariablePath = "/api/admin/configs"
        let localVariableURLString = ShipeasyAdminAPI.basePath + localVariablePath
        let localVariableParameters = JSONEncodingHelper.encodingParameters(forEncodableObject: createConfigRequest)

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            "Content-Type": "application/json",
            "X-Project-Id": xProjectId?.encodeToJSON(),
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<CreateConfigResponse>.Type = ShipeasyAdminAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "POST", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }

    /**
     Delete a dynamic config
     
     - parameter id: (path) Stable opaque config id (&#x60;cfg_…&#x60;) or the config&#39;s &#x60;name&#x60;. 
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - returns: DeleteConfigResponse
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func deleteConfig(id: String, xProjectId: String? = nil) async throws -> DeleteConfigResponse {
        return try await deleteConfigWithRequestBuilder(id: id, xProjectId: xProjectId).execute().body
    }

    /**
     Delete a dynamic config
     - DELETE /api/admin/configs/{id}
     - Soft-deletes the config and rebuilds the project's flags KV blob.  **Use case:** Tear down a config after its consumers have stopped reading it.
     - Bearer Token:
       - type: http
       - name: bearerSdkKey
     - parameter id: (path) Stable opaque config id (&#x60;cfg_…&#x60;) or the config&#39;s &#x60;name&#x60;. 
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - returns: RequestBuilder<DeleteConfigResponse> 
     */
    open class func deleteConfigWithRequestBuilder(id: String, xProjectId: String? = nil) -> RequestBuilder<DeleteConfigResponse> {
        var localVariablePath = "/api/admin/configs/{id}"
        let idPreEscape = "\(APIHelper.mapValueToPathItem(id))"
        let idPostEscape = idPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        localVariablePath = localVariablePath.replacingOccurrences(of: "{id}", with: idPostEscape, options: .literal, range: nil)
        let localVariableURLString = ShipeasyAdminAPI.basePath + localVariablePath
        let localVariableParameters: [String: Any]? = nil

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            "X-Project-Id": xProjectId?.encodeToJSON(),
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<DeleteConfigResponse>.Type = ShipeasyAdminAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "DELETE", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }

    /**
     Discard a draft
     
     - parameter id: (path) Stable opaque config id (&#x60;cfg_…&#x60;) or the config&#39;s &#x60;name&#x60;. 
     - parameter discardConfigDraftRequest: (body)  
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - returns: DiscardConfigDraftResponse
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func discardConfigDraft(id: String, discardConfigDraftRequest: DiscardConfigDraftRequest, xProjectId: String? = nil) async throws -> DiscardConfigDraftResponse {
        return try await discardConfigDraftWithRequestBuilder(id: id, discardConfigDraftRequest: discardConfigDraftRequest, xProjectId: xProjectId).execute().body
    }

    /**
     Discard a draft
     - DELETE /api/admin/configs/{id}/drafts
     - Drops the in-flight draft on one env. Published values are unaffected.  **Use case:** Abandon an in-progress draft after deciding not to ship it.
     - Bearer Token:
       - type: http
       - name: bearerSdkKey
     - parameter id: (path) Stable opaque config id (&#x60;cfg_…&#x60;) or the config&#39;s &#x60;name&#x60;. 
     - parameter discardConfigDraftRequest: (body)  
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - returns: RequestBuilder<DiscardConfigDraftResponse> 
     */
    open class func discardConfigDraftWithRequestBuilder(id: String, discardConfigDraftRequest: DiscardConfigDraftRequest, xProjectId: String? = nil) -> RequestBuilder<DiscardConfigDraftResponse> {
        var localVariablePath = "/api/admin/configs/{id}/drafts"
        let idPreEscape = "\(APIHelper.mapValueToPathItem(id))"
        let idPostEscape = idPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        localVariablePath = localVariablePath.replacingOccurrences(of: "{id}", with: idPostEscape, options: .literal, range: nil)
        let localVariableURLString = ShipeasyAdminAPI.basePath + localVariablePath
        let localVariableParameters = JSONEncodingHelper.encodingParameters(forEncodableObject: discardConfigDraftRequest)

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            "Content-Type": "application/json",
            "X-Project-Id": xProjectId?.encodeToJSON(),
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<DiscardConfigDraftResponse>.Type = ShipeasyAdminAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "DELETE", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }

    /**
     Get one config
     
     - parameter id: (path) Stable opaque config id (&#x60;cfg_…&#x60;) or the config&#39;s &#x60;name&#x60;. 
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - returns: GetConfigResponse
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func getConfig(id: String, xProjectId: String? = nil) async throws -> GetConfigResponse {
        return try await getConfigWithRequestBuilder(id: id, xProjectId: xProjectId).execute().body
    }

    /**
     Get one config
     - GET /api/admin/configs/{id}
     - Returns config metadata plus the latest published values per env and any active draft values. Use this to fetch the JSON the editor renders.  **Use case:** Fetch one config's current published values and any in-flight drafts.
     - Bearer Token:
       - type: http
       - name: bearerSdkKey
     - parameter id: (path) Stable opaque config id (&#x60;cfg_…&#x60;) or the config&#39;s &#x60;name&#x60;. 
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - returns: RequestBuilder<GetConfigResponse> 
     */
    open class func getConfigWithRequestBuilder(id: String, xProjectId: String? = nil) -> RequestBuilder<GetConfigResponse> {
        var localVariablePath = "/api/admin/configs/{id}"
        let idPreEscape = "\(APIHelper.mapValueToPathItem(id))"
        let idPostEscape = idPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        localVariablePath = localVariablePath.replacingOccurrences(of: "{id}", with: idPostEscape, options: .literal, range: nil)
        let localVariableURLString = ShipeasyAdminAPI.basePath + localVariablePath
        let localVariableParameters: [String: Any]? = nil

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            "X-Project-Id": xProjectId?.encodeToJSON(),
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<GetConfigResponse>.Type = ShipeasyAdminAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "GET", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }

    /**
     List config activity
     
     - parameter id: (path) Stable opaque config id (&#x60;cfg_…&#x60;) or the config&#39;s &#x60;name&#x60;. 
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - parameter limit: (query) Max rows to return (1–100). Defaults to 20. (optional, default to 20)
     - returns: [ListConfigActivityResponseInner]
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func listConfigActivity(id: String, xProjectId: String? = nil, limit: Int? = nil) async throws -> [ListConfigActivityResponseInner] {
        return try await listConfigActivityWithRequestBuilder(id: id, xProjectId: xProjectId, limit: limit).execute().body
    }

    /**
     List config activity
     - GET /api/admin/configs/{id}/activity
     - Returns recent audit rows for one config (create, update, draft.save, publish, delete) ordered newest first. Use the `limit` query parameter to cap the result (1–100, default 20).  **Use case:** Render the activity panel in the config editor or drive a slack notification on publish events.
     - Bearer Token:
       - type: http
       - name: bearerSdkKey
     - parameter id: (path) Stable opaque config id (&#x60;cfg_…&#x60;) or the config&#39;s &#x60;name&#x60;. 
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - parameter limit: (query) Max rows to return (1–100). Defaults to 20. (optional, default to 20)
     - returns: RequestBuilder<[ListConfigActivityResponseInner]> 
     */
    open class func listConfigActivityWithRequestBuilder(id: String, xProjectId: String? = nil, limit: Int? = nil) -> RequestBuilder<[ListConfigActivityResponseInner]> {
        var localVariablePath = "/api/admin/configs/{id}/activity"
        let idPreEscape = "\(APIHelper.mapValueToPathItem(id))"
        let idPostEscape = idPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        localVariablePath = localVariablePath.replacingOccurrences(of: "{id}", with: idPostEscape, options: .literal, range: nil)
        let localVariableURLString = ShipeasyAdminAPI.basePath + localVariablePath
        let localVariableParameters: [String: Any]? = nil

        var localVariableUrlComponents = URLComponents(string: localVariableURLString)
        localVariableUrlComponents?.queryItems = APIHelper.mapValuesToQueryItems([
            "limit": (wrappedValue: limit?.encodeToJSON(), isExplode: true),
        ])

        let localVariableNillableHeaders: [String: Any?] = [
            "X-Project-Id": xProjectId?.encodeToJSON(),
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<[ListConfigActivityResponseInner]>.Type = ShipeasyAdminAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "GET", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }

    /**
     List dynamic configs
     
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - parameter limit: (query) Page size (1–500). Defaults to 100. (optional, default to 100)
     - parameter cursor: (query) Opaque cursor returned in the previous page&#39;s &#x60;next_cursor&#x60;. Omit for the first page. (optional)
     - parameter q: (query) Case-insensitive substring filter across the resource&#39;s human-readable text columns (e.g. &#x60;name&#x60;, &#x60;title&#x60;, &#x60;description&#x60;). OR-matched across those columns; omit to return everything. (optional)
     - returns: ListConfigsResponse
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func listConfigs(xProjectId: String? = nil, limit: Int? = nil, cursor: String? = nil, q: String? = nil) async throws -> ListConfigsResponse {
        return try await listConfigsWithRequestBuilder(xProjectId: xProjectId, limit: limit, cursor: cursor, q: q).execute().body
    }

    /**
     List dynamic configs
     - GET /api/admin/configs
     - Returns a single page of configs ordered by `updated_at desc, id desc`. Each row includes the latest published `version` per env and any active drafts.  **Use case:** Snapshot every config in the project — e.g. CI check that asserts no env is stuck on a stale default or that every config has a published value on prod.
     - Bearer Token:
       - type: http
       - name: bearerSdkKey
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - parameter limit: (query) Page size (1–500). Defaults to 100. (optional, default to 100)
     - parameter cursor: (query) Opaque cursor returned in the previous page&#39;s &#x60;next_cursor&#x60;. Omit for the first page. (optional)
     - parameter q: (query) Case-insensitive substring filter across the resource&#39;s human-readable text columns (e.g. &#x60;name&#x60;, &#x60;title&#x60;, &#x60;description&#x60;). OR-matched across those columns; omit to return everything. (optional)
     - returns: RequestBuilder<ListConfigsResponse> 
     */
    open class func listConfigsWithRequestBuilder(xProjectId: String? = nil, limit: Int? = nil, cursor: String? = nil, q: String? = nil) -> RequestBuilder<ListConfigsResponse> {
        let localVariablePath = "/api/admin/configs"
        let localVariableURLString = ShipeasyAdminAPI.basePath + localVariablePath
        let localVariableParameters: [String: Any]? = nil

        var localVariableUrlComponents = URLComponents(string: localVariableURLString)
        localVariableUrlComponents?.queryItems = APIHelper.mapValuesToQueryItems([
            "limit": (wrappedValue: limit?.encodeToJSON(), isExplode: true),
            "cursor": (wrappedValue: cursor?.encodeToJSON(), isExplode: true),
            "q": (wrappedValue: q?.encodeToJSON(), isExplode: true),
        ])

        let localVariableNillableHeaders: [String: Any?] = [
            "X-Project-Id": xProjectId?.encodeToJSON(),
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<ListConfigsResponse>.Type = ShipeasyAdminAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "GET", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }

    /**
     Publish a draft
     
     - parameter id: (path) Stable opaque config id (&#x60;cfg_…&#x60;) or the config&#39;s &#x60;name&#x60;. 
     - parameter publishConfigDraftRequest: (body)  
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - returns: PublishConfigDraftResponse
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func publishConfigDraft(id: String, publishConfigDraftRequest: PublishConfigDraftRequest, xProjectId: String? = nil) async throws -> PublishConfigDraftResponse {
        return try await publishConfigDraftWithRequestBuilder(id: id, publishConfigDraftRequest: publishConfigDraftRequest, xProjectId: xProjectId).execute().body
    }

    /**
     Publish a draft
     - POST /api/admin/configs/{id}/publish
     - Promotes the staged draft on one env to a new published version. The draft must still validate against the current schema.  Returns `404` if there is no draft for the given env.  **Use case:** Ship a staged change once you've validated it on a lower env.
     - Bearer Token:
       - type: http
       - name: bearerSdkKey
     - parameter id: (path) Stable opaque config id (&#x60;cfg_…&#x60;) or the config&#39;s &#x60;name&#x60;. 
     - parameter publishConfigDraftRequest: (body)  
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - returns: RequestBuilder<PublishConfigDraftResponse> 
     */
    open class func publishConfigDraftWithRequestBuilder(id: String, publishConfigDraftRequest: PublishConfigDraftRequest, xProjectId: String? = nil) -> RequestBuilder<PublishConfigDraftResponse> {
        var localVariablePath = "/api/admin/configs/{id}/publish"
        let idPreEscape = "\(APIHelper.mapValueToPathItem(id))"
        let idPostEscape = idPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        localVariablePath = localVariablePath.replacingOccurrences(of: "{id}", with: idPostEscape, options: .literal, range: nil)
        let localVariableURLString = ShipeasyAdminAPI.basePath + localVariablePath
        let localVariableParameters = JSONEncodingHelper.encodingParameters(forEncodableObject: publishConfigDraftRequest)

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            "Content-Type": "application/json",
            "X-Project-Id": xProjectId?.encodeToJSON(),
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<PublishConfigDraftResponse>.Type = ShipeasyAdminAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "POST", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }

    /**
     Save a draft value
     
     - parameter id: (path) Stable opaque config id (&#x60;cfg_…&#x60;) or the config&#39;s &#x60;name&#x60;. 
     - parameter saveConfigDraftRequest: (body)  
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - returns: SaveConfigDraftResponse
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func saveConfigDraft(id: String, saveConfigDraftRequest: SaveConfigDraftRequest, xProjectId: String? = nil) async throws -> SaveConfigDraftResponse {
        return try await saveConfigDraftWithRequestBuilder(id: id, saveConfigDraftRequest: saveConfigDraftRequest, xProjectId: xProjectId).execute().body
    }

    /**
     Save a draft value
     - PUT /api/admin/configs/{id}/drafts
     - Stages a value for one env without publishing. The draft is validated against the config's current schema and stored alongside the `baseVersion` it was forked from.  Saving over an existing draft overwrites it. Use `POST /{id}/publish` to promote it to a new published version.  **Use case:** Iterate on a config value on dev without affecting prod — preview in staging, then publish.
     - Bearer Token:
       - type: http
       - name: bearerSdkKey
     - parameter id: (path) Stable opaque config id (&#x60;cfg_…&#x60;) or the config&#39;s &#x60;name&#x60;. 
     - parameter saveConfigDraftRequest: (body)  
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - returns: RequestBuilder<SaveConfigDraftResponse> 
     */
    open class func saveConfigDraftWithRequestBuilder(id: String, saveConfigDraftRequest: SaveConfigDraftRequest, xProjectId: String? = nil) -> RequestBuilder<SaveConfigDraftResponse> {
        var localVariablePath = "/api/admin/configs/{id}/drafts"
        let idPreEscape = "\(APIHelper.mapValueToPathItem(id))"
        let idPostEscape = idPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        localVariablePath = localVariablePath.replacingOccurrences(of: "{id}", with: idPostEscape, options: .literal, range: nil)
        let localVariableURLString = ShipeasyAdminAPI.basePath + localVariablePath
        let localVariableParameters = JSONEncodingHelper.encodingParameters(forEncodableObject: saveConfigDraftRequest)

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            "Content-Type": "application/json",
            "X-Project-Id": xProjectId?.encodeToJSON(),
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<SaveConfigDraftResponse>.Type = ShipeasyAdminAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "PUT", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }

    /**
     Update a dynamic config
     
     - parameter id: (path) Stable opaque config id (&#x60;cfg_…&#x60;) or the config&#39;s &#x60;name&#x60;. 
     - parameter updateConfigRequest: (body)  
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - returns: UpdateConfigResponse
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func updateConfig(id: String, updateConfigRequest: UpdateConfigRequest, xProjectId: String? = nil) async throws -> UpdateConfigResponse {
        return try await updateConfigWithRequestBuilder(id: id, updateConfigRequest: updateConfigRequest, xProjectId: xProjectId).execute().body
    }

    /**
     Update a dynamic config
     - PATCH /api/admin/configs/{id}
     - Partial update. When `value` is supplied it is **republished on every env** (new version per env). When `schema` is supplied it replaces the current schema; every existing value is re-validated.  For env-scoped edits, use the draft/publish flow (`PUT /{id}/drafts` then `POST /{id}/publish`) instead.  **Use cases**  - **Republish flat value** — `{ \"value\": {…} }` sets the same value on every env. - **Schema migration** — `{ \"schema\": {…} }` replaces the schema; existing values are re-validated. - **Env-scoped edits** — use `PUT /{id}/drafts` + `POST /{id}/publish` instead of PATCH.
     - Bearer Token:
       - type: http
       - name: bearerSdkKey
     - parameter id: (path) Stable opaque config id (&#x60;cfg_…&#x60;) or the config&#39;s &#x60;name&#x60;. 
     - parameter updateConfigRequest: (body)  
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - returns: RequestBuilder<UpdateConfigResponse> 
     */
    open class func updateConfigWithRequestBuilder(id: String, updateConfigRequest: UpdateConfigRequest, xProjectId: String? = nil) -> RequestBuilder<UpdateConfigResponse> {
        var localVariablePath = "/api/admin/configs/{id}"
        let idPreEscape = "\(APIHelper.mapValueToPathItem(id))"
        let idPostEscape = idPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        localVariablePath = localVariablePath.replacingOccurrences(of: "{id}", with: idPostEscape, options: .literal, range: nil)
        let localVariableURLString = ShipeasyAdminAPI.basePath + localVariablePath
        let localVariableParameters = JSONEncodingHelper.encodingParameters(forEncodableObject: updateConfigRequest)

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            "Content-Type": "application/json",
            "X-Project-Id": xProjectId?.encodeToJSON(),
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<UpdateConfigResponse>.Type = ShipeasyAdminAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "PATCH", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }

    /**
     Update a config schema
     
     - parameter id: (path) Stable opaque config id (&#x60;cfg_…&#x60;) or the config&#39;s &#x60;name&#x60;. 
     - parameter updateConfigSchemaRequest: (body)  
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - returns: UpdateConfigSchemaResponse
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func updateConfigSchema(id: String, updateConfigSchemaRequest: UpdateConfigSchemaRequest, xProjectId: String? = nil) async throws -> UpdateConfigSchemaResponse {
        return try await updateConfigSchemaWithRequestBuilder(id: id, updateConfigSchemaRequest: updateConfigSchemaRequest, xProjectId: xProjectId).execute().body
    }

    /**
     Update a config schema
     - PATCH /api/admin/configs/{id}/schema
     - Replaces a config's JSON Schema in place. Every existing published value is re-validated against the new schema before it lands; the update fails if any value no longer validates.  **Use case:** Evolve a config's shape (add/remove a field) without republishing values.
     - Bearer Token:
       - type: http
       - name: bearerSdkKey
     - parameter id: (path) Stable opaque config id (&#x60;cfg_…&#x60;) or the config&#39;s &#x60;name&#x60;. 
     - parameter updateConfigSchemaRequest: (body)  
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - returns: RequestBuilder<UpdateConfigSchemaResponse> 
     */
    open class func updateConfigSchemaWithRequestBuilder(id: String, updateConfigSchemaRequest: UpdateConfigSchemaRequest, xProjectId: String? = nil) -> RequestBuilder<UpdateConfigSchemaResponse> {
        var localVariablePath = "/api/admin/configs/{id}/schema"
        let idPreEscape = "\(APIHelper.mapValueToPathItem(id))"
        let idPostEscape = idPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        localVariablePath = localVariablePath.replacingOccurrences(of: "{id}", with: idPostEscape, options: .literal, range: nil)
        let localVariableURLString = ShipeasyAdminAPI.basePath + localVariablePath
        let localVariableParameters = JSONEncodingHelper.encodingParameters(forEncodableObject: updateConfigSchemaRequest)

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            "Content-Type": "application/json",
            "X-Project-Id": xProjectId?.encodeToJSON(),
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<UpdateConfigSchemaResponse>.Type = ShipeasyAdminAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "PATCH", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }
}
