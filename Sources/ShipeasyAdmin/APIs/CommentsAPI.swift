//
// CommentsAPI.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

open class CommentsAPI {

    /**
     Comment on an item
     
     - parameter handle: (path) Per-project item number (e.g. &#x60;7&#x60;) or the full ops item id. 
     - parameter createOpsCommentRequest: (body)  
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - returns: CreateOpsCommentResponse
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func createOpsComment(handle: String, createOpsCommentRequest: CreateOpsCommentRequest, xProjectId: String? = nil) async throws -> CreateOpsCommentResponse {
        return try await createOpsCommentWithRequestBuilder(handle: handle, createOpsCommentRequest: createOpsCommentRequest, xProjectId: xProjectId).execute().body
    }

    /**
     Comment on an item
     - POST /api/admin/ops/{handle}/comments
     - Append a comment to a queue item's thread. The body is markdown (mentions like `@teammate` notify that person; `@shipeasy` asks Jarvis, the AI agent, to reply). Pass `parentId` to reply under an existing top-level comment (one level of threading — a reply to a reply attaches to the same parent).  Create-only and append-only, so it is safe for restricted ops keys — the same channel the ops loop and Jarvis use to leave a note on an item.
     - Bearer Token:
       - type: http
       - name: bearerSdkKey
     - parameter handle: (path) Per-project item number (e.g. &#x60;7&#x60;) or the full ops item id. 
     - parameter createOpsCommentRequest: (body)  
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - returns: RequestBuilder<CreateOpsCommentResponse> 
     */
    open class func createOpsCommentWithRequestBuilder(handle: String, createOpsCommentRequest: CreateOpsCommentRequest, xProjectId: String? = nil) -> RequestBuilder<CreateOpsCommentResponse> {
        var localVariablePath = "/api/admin/ops/{handle}/comments"
        let handlePreEscape = "\(APIHelper.mapValueToPathItem(handle))"
        let handlePostEscape = handlePreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        localVariablePath = localVariablePath.replacingOccurrences(of: "{handle}", with: handlePostEscape, options: .literal, range: nil)
        let localVariableURLString = ShipeasyAdminAPI.basePath + localVariablePath
        let localVariableParameters = JSONEncodingHelper.encodingParameters(forEncodableObject: createOpsCommentRequest)

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            "Content-Type": "application/json",
            "X-Project-Id": xProjectId?.encodeToJSON(),
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<CreateOpsCommentResponse>.Type = ShipeasyAdminAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "POST", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }

    /**
     List an item's comments
     
     - parameter handle: (path) Per-project item number (e.g. &#x60;7&#x60;) or the full ops item id. 
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - returns: ListOpsCommentsResponse
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func listOpsComments(handle: String, xProjectId: String? = nil) async throws -> ListOpsCommentsResponse {
        return try await listOpsCommentsWithRequestBuilder(handle: handle, xProjectId: xProjectId).execute().body
    }

    /**
     List an item's comments
     - GET /api/admin/ops/{handle}/comments
     - List the comment thread on a queue item, oldest first. Each comment carries its author (a teammate email, or `system` for a comment authored by Jarvis — the AI agent), its markdown body, and `parentId` for the single level of threaded replies. Removed comments are omitted.  **Use case:** Read the discussion on an item before replying or acting on it.
     - Bearer Token:
       - type: http
       - name: bearerSdkKey
     - parameter handle: (path) Per-project item number (e.g. &#x60;7&#x60;) or the full ops item id. 
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - returns: RequestBuilder<ListOpsCommentsResponse> 
     */
    open class func listOpsCommentsWithRequestBuilder(handle: String, xProjectId: String? = nil) -> RequestBuilder<ListOpsCommentsResponse> {
        var localVariablePath = "/api/admin/ops/{handle}/comments"
        let handlePreEscape = "\(APIHelper.mapValueToPathItem(handle))"
        let handlePostEscape = handlePreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        localVariablePath = localVariablePath.replacingOccurrences(of: "{handle}", with: handlePostEscape, options: .literal, range: nil)
        let localVariableURLString = ShipeasyAdminAPI.basePath + localVariablePath
        let localVariableParameters: [String: Any]? = nil

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            "X-Project-Id": xProjectId?.encodeToJSON(),
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<ListOpsCommentsResponse>.Type = ShipeasyAdminAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "GET", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }
}
