//
// KeysAPI.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

open class KeysAPI {

    /**
     List i18n keys
     
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - parameter profileId: (query) Profile id to list keys for. (optional)
     - parameter _prefix: (query) Only keys whose name starts with this. (optional)
     - parameter q: (query) Free-text search — matches keys whose name, value, OR description contains this substring (case-insensitive). Use it to find the key behind a piece of on-screen copy. (optional)
     - parameter limit: (query) Max keys to return (1–500). (optional)
     - parameter offset: (query) Number of keys to skip before returning &#x60;limit&#x60; rows (offset pagination). (optional, default to 0)
     - returns: ListI18nKeysResponse
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func listI18nKeys(xProjectId: String? = nil, profileId: String? = nil, _prefix: String? = nil, q: String? = nil, limit: Int? = nil, offset: Int? = nil) async throws -> ListI18nKeysResponse {
        return try await listI18nKeysWithRequestBuilder(xProjectId: xProjectId, profileId: profileId, _prefix: _prefix, q: q, limit: limit, offset: offset).execute().body
    }

    /**
     List i18n keys
     - GET /api/admin/i18n/keys
     - List keys for a profile, optionally filtered to a name `prefix`.  **Use case:** Read the current keys (and values) for a profile — e.g. to diff before a push.
     - Bearer Token:
       - type: http
       - name: bearerSdkKey
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - parameter profileId: (query) Profile id to list keys for. (optional)
     - parameter _prefix: (query) Only keys whose name starts with this. (optional)
     - parameter q: (query) Free-text search — matches keys whose name, value, OR description contains this substring (case-insensitive). Use it to find the key behind a piece of on-screen copy. (optional)
     - parameter limit: (query) Max keys to return (1–500). (optional)
     - parameter offset: (query) Number of keys to skip before returning &#x60;limit&#x60; rows (offset pagination). (optional, default to 0)
     - returns: RequestBuilder<ListI18nKeysResponse> 
     */
    open class func listI18nKeysWithRequestBuilder(xProjectId: String? = nil, profileId: String? = nil, _prefix: String? = nil, q: String? = nil, limit: Int? = nil, offset: Int? = nil) -> RequestBuilder<ListI18nKeysResponse> {
        let localVariablePath = "/api/admin/i18n/keys"
        let localVariableURLString = ShipeasyAdminAPI.basePath + localVariablePath
        let localVariableParameters: [String: Any]? = nil

        var localVariableUrlComponents = URLComponents(string: localVariableURLString)
        localVariableUrlComponents?.queryItems = APIHelper.mapValuesToQueryItems([
            "profile_id": (wrappedValue: profileId?.encodeToJSON(), isExplode: true),
            "prefix": (wrappedValue: _prefix?.encodeToJSON(), isExplode: true),
            "q": (wrappedValue: q?.encodeToJSON(), isExplode: true),
            "limit": (wrappedValue: limit?.encodeToJSON(), isExplode: true),
            "offset": (wrappedValue: offset?.encodeToJSON(), isExplode: true),
        ])

        let localVariableNillableHeaders: [String: Any?] = [
            "X-Project-Id": xProjectId?.encodeToJSON(),
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<ListI18nKeysResponse>.Type = ShipeasyAdminAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "GET", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }

    /**
     Push new i18n keys (insert-only)
     
     - parameter pushI18nKeysRequest: (body)  
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - returns: PushI18nKeysResponse
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func pushI18nKeys(pushI18nKeysRequest: PushI18nKeysRequest, xProjectId: String? = nil) async throws -> PushI18nKeysResponse {
        return try await pushI18nKeysWithRequestBuilder(pushI18nKeysRequest: pushI18nKeysRequest, xProjectId: xProjectId).execute().body
    }

    /**
     Push new i18n keys (insert-only)
     - POST /api/admin/i18n/keys
     - Add NEW keys to a profile. Insert-only — existing keys are left untouched (overwrite one with `updateI18nKey`).  **Use case:** Seed newly-extracted keys without clobbering translations already in the profile.
     - Bearer Token:
       - type: http
       - name: bearerSdkKey
     - parameter pushI18nKeysRequest: (body)  
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - returns: RequestBuilder<PushI18nKeysResponse> 
     */
    open class func pushI18nKeysWithRequestBuilder(pushI18nKeysRequest: PushI18nKeysRequest, xProjectId: String? = nil) -> RequestBuilder<PushI18nKeysResponse> {
        let localVariablePath = "/api/admin/i18n/keys"
        let localVariableURLString = ShipeasyAdminAPI.basePath + localVariablePath
        let localVariableParameters = JSONEncodingHelper.encodingParameters(forEncodableObject: pushI18nKeysRequest)

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            "Content-Type": "application/json",
            "X-Project-Id": xProjectId?.encodeToJSON(),
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<PushI18nKeysResponse>.Type = ShipeasyAdminAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "POST", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }

    /**
     Set a key's value and publish it live
     
     - parameter setI18nLabelRequest: (body)  
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - returns: SetI18nLabelResponse
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func setI18nLabel(setI18nLabelRequest: SetI18nLabelRequest, xProjectId: String? = nil) async throws -> SetI18nLabelResponse {
        return try await setI18nLabelWithRequestBuilder(setI18nLabelRequest: setI18nLabelRequest, xProjectId: xProjectId).execute().body
    }

    /**
     Set a key's value and publish it live
     - POST /api/admin/i18n/set
     - Upsert a single key's value into a profile and immediately publish the whole profile (KV rebuild + CDN purge) so the new value is live in one call. The key is inserted when new and overwritten when it already exists. `profile` is a profile name — omit it to target the project's default-marked profile, or pass a name to target another locale.  **Use case:** Correct or replace one live string and ship it without a separate `push` + `publish`.
     - Bearer Token:
       - type: http
       - name: bearerSdkKey
     - parameter setI18nLabelRequest: (body)  
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - returns: RequestBuilder<SetI18nLabelResponse> 
     */
    open class func setI18nLabelWithRequestBuilder(setI18nLabelRequest: SetI18nLabelRequest, xProjectId: String? = nil) -> RequestBuilder<SetI18nLabelResponse> {
        let localVariablePath = "/api/admin/i18n/set"
        let localVariableURLString = ShipeasyAdminAPI.basePath + localVariablePath
        let localVariableParameters = JSONEncodingHelper.encodingParameters(forEncodableObject: setI18nLabelRequest)

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            "Content-Type": "application/json",
            "X-Project-Id": xProjectId?.encodeToJSON(),
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<SetI18nLabelResponse>.Type = ShipeasyAdminAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "POST", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }

    /**
     Update one i18n key
     
     - parameter id: (path) The key&#39;s id. 
     - parameter updateI18nKeyRequest: (body)  
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - returns: UpdateI18nKeyResponse
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func updateI18nKey(id: String, updateI18nKeyRequest: UpdateI18nKeyRequest, xProjectId: String? = nil) async throws -> UpdateI18nKeyResponse {
        return try await updateI18nKeyWithRequestBuilder(id: id, updateI18nKeyRequest: updateI18nKeyRequest, xProjectId: xProjectId).execute().body
    }

    /**
     Update one i18n key
     - PUT /api/admin/i18n/keys/{id}
     - Overwrite a single existing key's value — the only overwrite path.  The change goes live on its own: the profile's KV snapshot is rebuilt and the CDN purged as part of this call, so no separate profile publish is needed.  **Use case:** Correct or re-translate a single string in place.
     - Bearer Token:
       - type: http
       - name: bearerSdkKey
     - parameter id: (path) The key&#39;s id. 
     - parameter updateI18nKeyRequest: (body)  
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - returns: RequestBuilder<UpdateI18nKeyResponse> 
     */
    open class func updateI18nKeyWithRequestBuilder(id: String, updateI18nKeyRequest: UpdateI18nKeyRequest, xProjectId: String? = nil) -> RequestBuilder<UpdateI18nKeyResponse> {
        var localVariablePath = "/api/admin/i18n/keys/{id}"
        let idPreEscape = "\(APIHelper.mapValueToPathItem(id))"
        let idPostEscape = idPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        localVariablePath = localVariablePath.replacingOccurrences(of: "{id}", with: idPostEscape, options: .literal, range: nil)
        let localVariableURLString = ShipeasyAdminAPI.basePath + localVariablePath
        let localVariableParameters = JSONEncodingHelper.encodingParameters(forEncodableObject: updateI18nKeyRequest)

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            "Content-Type": "application/json",
            "X-Project-Id": xProjectId?.encodeToJSON(),
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<UpdateI18nKeyResponse>.Type = ShipeasyAdminAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "PUT", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }
}
