//
// TemplatesAPI.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

open class TemplatesAPI {

    /**
     Create a gate template
     
     - parameter createGateTemplateRequest: (body)  
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - returns: CreateGateTemplateResponse
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func createGateTemplate(createGateTemplateRequest: CreateGateTemplateRequest, xProjectId: String? = nil) async throws -> CreateGateTemplateResponse {
        return try await createGateTemplateWithRequestBuilder(createGateTemplateRequest: createGateTemplateRequest, xProjectId: xProjectId).execute().body
    }

    /**
     Create a gate template
     - POST /api/admin/gates/templates
     - Creates a per-project (customer) targeting-rule template. Built-ins are read-only and cannot be created here. Returns `409` if a template with this `name` already exists in the project.
     - Bearer Token:
       - type: http
       - name: bearerSdkKey
     - parameter createGateTemplateRequest: (body)  
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - returns: RequestBuilder<CreateGateTemplateResponse> 
     */
    open class func createGateTemplateWithRequestBuilder(createGateTemplateRequest: CreateGateTemplateRequest, xProjectId: String? = nil) -> RequestBuilder<CreateGateTemplateResponse> {
        let localVariablePath = "/api/admin/gates/templates"
        let localVariableURLString = ShipeasyAdminAPI.basePath + localVariablePath
        let localVariableParameters = JSONEncodingHelper.encodingParameters(forEncodableObject: createGateTemplateRequest)

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            "Content-Type": "application/json",
            "X-Project-Id": xProjectId?.encodeToJSON(),
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<CreateGateTemplateResponse>.Type = ShipeasyAdminAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "POST", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }

    /**
     Delete a gate template
     
     - parameter id: (path) Customer template id (&#x60;gtpl_…&#x60;) or its &#x60;name&#x60;. 
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - returns: DeleteGateTemplateResponse
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func deleteGateTemplate(id: String, xProjectId: String? = nil) async throws -> DeleteGateTemplateResponse {
        return try await deleteGateTemplateWithRequestBuilder(id: id, xProjectId: xProjectId).execute().body
    }

    /**
     Delete a gate template
     - DELETE /api/admin/gates/templates/{id}
     - Soft-deletes (archives) a **customer** template. Returns `409` if `id` names a read-only built-in template.
     - Bearer Token:
       - type: http
       - name: bearerSdkKey
     - parameter id: (path) Customer template id (&#x60;gtpl_…&#x60;) or its &#x60;name&#x60;. 
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - returns: RequestBuilder<DeleteGateTemplateResponse> 
     */
    open class func deleteGateTemplateWithRequestBuilder(id: String, xProjectId: String? = nil) -> RequestBuilder<DeleteGateTemplateResponse> {
        var localVariablePath = "/api/admin/gates/templates/{id}"
        let idPreEscape = "\(APIHelper.mapValueToPathItem(id))"
        let idPostEscape = idPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        localVariablePath = localVariablePath.replacingOccurrences(of: "{id}", with: idPostEscape, options: .literal, range: nil)
        let localVariableURLString = ShipeasyAdminAPI.basePath + localVariablePath
        let localVariableParameters: [String: Any]? = nil

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            "X-Project-Id": xProjectId?.encodeToJSON(),
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<DeleteGateTemplateResponse>.Type = ShipeasyAdminAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "DELETE", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }

    /**
     Get one gate template
     
     - parameter id: (path) Built-in slug (&#x60;country&#x60;) or customer template id (&#x60;gtpl_…&#x60;). 
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - returns: GateTemplate
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func getGateTemplate(id: String, xProjectId: String? = nil) async throws -> GateTemplate {
        return try await getGateTemplateWithRequestBuilder(id: id, xProjectId: xProjectId).execute().body
    }

    /**
     Get one gate template
     - GET /api/admin/gates/templates/{id}
     - Returns a single template by its `id` — a built-in slug (`country`) or a customer `gtpl_…` id.
     - Bearer Token:
       - type: http
       - name: bearerSdkKey
     - parameter id: (path) Built-in slug (&#x60;country&#x60;) or customer template id (&#x60;gtpl_…&#x60;). 
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - returns: RequestBuilder<GateTemplate> 
     */
    open class func getGateTemplateWithRequestBuilder(id: String, xProjectId: String? = nil) -> RequestBuilder<GateTemplate> {
        var localVariablePath = "/api/admin/gates/templates/{id}"
        let idPreEscape = "\(APIHelper.mapValueToPathItem(id))"
        let idPostEscape = idPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        localVariablePath = localVariablePath.replacingOccurrences(of: "{id}", with: idPostEscape, options: .literal, range: nil)
        let localVariableURLString = ShipeasyAdminAPI.basePath + localVariablePath
        let localVariableParameters: [String: Any]? = nil

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            "X-Project-Id": xProjectId?.encodeToJSON(),
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<GateTemplate>.Type = ShipeasyAdminAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "GET", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }

    /**
     List gate templates
     
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - parameter q: (query) Case-insensitive substring filter across the resource&#39;s human-readable text columns (e.g. &#x60;name&#x60;, &#x60;title&#x60;, &#x60;description&#x60;). OR-matched across those columns; omit to return everything. (optional)
     - parameter query: (query) Deprecated alias for &#x60;q&#x60;, kept working for one release. Prefer &#x60;q&#x60;. (optional)
     - returns: ListGateTemplatesResponse
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func listGateTemplates(xProjectId: String? = nil, q: String? = nil, query: String? = nil) async throws -> ListGateTemplatesResponse {
        return try await listGateTemplatesWithRequestBuilder(xProjectId: xProjectId, q: q, query: query).execute().body
    }

    /**
     List gate templates
     - GET /api/admin/gates/templates
     - Returns the merged targeting-rule template catalog — read-only **built-ins first** (the `@team`/`@owner` audience aliases, country, email-domain, region presets, …), then this project's **customer** templates. Each item carries a `rules: [{ attr, op, value }]` definition; the alias templates carry a symbol value (`email in [\"@team\"]`) that the SDK expands to the resolved email list at rebuild.  **Turn \"launch X in country Y\" into a gated flag:** list templates (optionally with `q`), take the matching template's `rules`, substitute the concrete value(s), and pass them as the `rules` arg of `release_flags_create`.  The catalog is small and bounded, so the full merged set is returned in one page — this endpoint is **not** paginated (`next_cursor` is always `null`; there are no `limit`/`cursor` params).
     - Bearer Token:
       - type: http
       - name: bearerSdkKey
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - parameter q: (query) Case-insensitive substring filter across the resource&#39;s human-readable text columns (e.g. &#x60;name&#x60;, &#x60;title&#x60;, &#x60;description&#x60;). OR-matched across those columns; omit to return everything. (optional)
     - parameter query: (query) Deprecated alias for &#x60;q&#x60;, kept working for one release. Prefer &#x60;q&#x60;. (optional)
     - returns: RequestBuilder<ListGateTemplatesResponse> 
     */
    open class func listGateTemplatesWithRequestBuilder(xProjectId: String? = nil, q: String? = nil, query: String? = nil) -> RequestBuilder<ListGateTemplatesResponse> {
        let localVariablePath = "/api/admin/gates/templates"
        let localVariableURLString = ShipeasyAdminAPI.basePath + localVariablePath
        let localVariableParameters: [String: Any]? = nil

        var localVariableUrlComponents = URLComponents(string: localVariableURLString)
        localVariableUrlComponents?.queryItems = APIHelper.mapValuesToQueryItems([
            "q": (wrappedValue: q?.encodeToJSON(), isExplode: true),
            "query": (wrappedValue: query?.encodeToJSON(), isExplode: true),
        ])

        let localVariableNillableHeaders: [String: Any?] = [
            "X-Project-Id": xProjectId?.encodeToJSON(),
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<ListGateTemplatesResponse>.Type = ShipeasyAdminAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "GET", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }

    /**
     Update a gate template
     
     - parameter id: (path) Customer template id (&#x60;gtpl_…&#x60;) or its &#x60;name&#x60;. 
     - parameter updateGateTemplateRequest: (body)  
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - returns: UpdateGateTemplateResponse
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func updateGateTemplate(id: String, updateGateTemplateRequest: UpdateGateTemplateRequest, xProjectId: String? = nil) async throws -> UpdateGateTemplateResponse {
        return try await updateGateTemplateWithRequestBuilder(id: id, updateGateTemplateRequest: updateGateTemplateRequest, xProjectId: xProjectId).execute().body
    }

    /**
     Update a gate template
     - PATCH /api/admin/gates/templates/{id}
     - Partial update of a **customer** template. `rules` replaces the list wholesale. Returns `409` if `id` names a read-only built-in template.
     - Bearer Token:
       - type: http
       - name: bearerSdkKey
     - parameter id: (path) Customer template id (&#x60;gtpl_…&#x60;) or its &#x60;name&#x60;. 
     - parameter updateGateTemplateRequest: (body)  
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - returns: RequestBuilder<UpdateGateTemplateResponse> 
     */
    open class func updateGateTemplateWithRequestBuilder(id: String, updateGateTemplateRequest: UpdateGateTemplateRequest, xProjectId: String? = nil) -> RequestBuilder<UpdateGateTemplateResponse> {
        var localVariablePath = "/api/admin/gates/templates/{id}"
        let idPreEscape = "\(APIHelper.mapValueToPathItem(id))"
        let idPostEscape = idPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        localVariablePath = localVariablePath.replacingOccurrences(of: "{id}", with: idPostEscape, options: .literal, range: nil)
        let localVariableURLString = ShipeasyAdminAPI.basePath + localVariablePath
        let localVariableParameters = JSONEncodingHelper.encodingParameters(forEncodableObject: updateGateTemplateRequest)

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            "Content-Type": "application/json",
            "X-Project-Id": xProjectId?.encodeToJSON(),
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<UpdateGateTemplateResponse>.Type = ShipeasyAdminAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "PATCH", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }
}
