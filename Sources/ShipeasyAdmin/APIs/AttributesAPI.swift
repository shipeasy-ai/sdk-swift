//
// AttributesAPI.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

open class AttributesAPI {

    /**
     Declare a targeting attribute
     
     - parameter createAttributeRequest: (body)  
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - returns: CreateAttributeResponse
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func createAttribute(createAttributeRequest: CreateAttributeRequest, xProjectId: String? = nil) async throws -> CreateAttributeResponse {
        return try await createAttributeWithRequestBuilder(createAttributeRequest: createAttributeRequest, xProjectId: xProjectId).execute().body
    }

    /**
     Declare a targeting attribute
     - POST /api/admin/attributes
     - Declare a targeting attribute the SDK reports and gates/experiments can target. `type: enum` requires `enum_values`.  **Use case:** Register a `plan` or `country` attribute so targeting rules can reference it.
     - Bearer Token:
       - type: http
       - name: bearerSdkKey
     - parameter createAttributeRequest: (body)  
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - returns: RequestBuilder<CreateAttributeResponse> 
     */
    open class func createAttributeWithRequestBuilder(createAttributeRequest: CreateAttributeRequest, xProjectId: String? = nil) -> RequestBuilder<CreateAttributeResponse> {
        let localVariablePath = "/api/admin/attributes"
        let localVariableURLString = ShipeasyAdminAPI.basePath + localVariablePath
        let localVariableParameters = JSONEncodingHelper.encodingParameters(forEncodableObject: createAttributeRequest)

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            "Content-Type": "application/json",
            "X-Project-Id": xProjectId?.encodeToJSON(),
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<CreateAttributeResponse>.Type = ShipeasyAdminAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "POST", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }

    /**
     Archive a targeting attribute
     
     - parameter id: (path) The attribute id. 
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - returns: DeleteAttributeResponse
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func deleteAttribute(id: String, xProjectId: String? = nil) async throws -> DeleteAttributeResponse {
        return try await deleteAttributeWithRequestBuilder(id: id, xProjectId: xProjectId).execute().body
    }

    /**
     Archive a targeting attribute
     - DELETE /api/admin/attributes/{id}
     - Soft-deletes (archives) a targeting attribute.  **Use case:** Retire an attribute no targeting rule references anymore (the user-facing verb is `archive`).
     - Bearer Token:
       - type: http
       - name: bearerSdkKey
     - parameter id: (path) The attribute id. 
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - returns: RequestBuilder<DeleteAttributeResponse> 
     */
    open class func deleteAttributeWithRequestBuilder(id: String, xProjectId: String? = nil) -> RequestBuilder<DeleteAttributeResponse> {
        var localVariablePath = "/api/admin/attributes/{id}"
        let idPreEscape = "\(APIHelper.mapValueToPathItem(id))"
        let idPostEscape = idPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        localVariablePath = localVariablePath.replacingOccurrences(of: "{id}", with: idPostEscape, options: .literal, range: nil)
        let localVariableURLString = ShipeasyAdminAPI.basePath + localVariablePath
        let localVariableParameters: [String: Any]? = nil

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            "X-Project-Id": xProjectId?.encodeToJSON(),
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<DeleteAttributeResponse>.Type = ShipeasyAdminAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "DELETE", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }

    /**
     Get a targeting attribute
     
     - parameter id: (path) The attribute id. 
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - returns: GetAttributeResponse
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func getAttribute(id: String, xProjectId: String? = nil) async throws -> GetAttributeResponse {
        return try await getAttributeWithRequestBuilder(id: id, xProjectId: xProjectId).execute().body
    }

    /**
     Get a targeting attribute
     - GET /api/admin/attributes/{id}
     - Fetch one targeting attribute by id.  **Use case:** Inspect an attribute's declared type + allowed values before editing it.
     - Bearer Token:
       - type: http
       - name: bearerSdkKey
     - parameter id: (path) The attribute id. 
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - returns: RequestBuilder<GetAttributeResponse> 
     */
    open class func getAttributeWithRequestBuilder(id: String, xProjectId: String? = nil) -> RequestBuilder<GetAttributeResponse> {
        var localVariablePath = "/api/admin/attributes/{id}"
        let idPreEscape = "\(APIHelper.mapValueToPathItem(id))"
        let idPostEscape = idPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        localVariablePath = localVariablePath.replacingOccurrences(of: "{id}", with: idPostEscape, options: .literal, range: nil)
        let localVariableURLString = ShipeasyAdminAPI.basePath + localVariablePath
        let localVariableParameters: [String: Any]? = nil

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            "X-Project-Id": xProjectId?.encodeToJSON(),
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<GetAttributeResponse>.Type = ShipeasyAdminAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "GET", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }

    /**
     List targeting attributes
     
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - parameter q: (query) Case-insensitive substring filter across the resource&#39;s human-readable text columns (e.g. &#x60;name&#x60;, &#x60;title&#x60;, &#x60;description&#x60;). OR-matched across those columns; omit to return everything. (optional)
     - returns: [ListAttributesResponseInner]
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func listAttributes(xProjectId: String? = nil, q: String? = nil) async throws -> [ListAttributesResponseInner] {
        return try await listAttributesWithRequestBuilder(xProjectId: xProjectId, q: q).execute().body
    }

    /**
     List targeting attributes
     - GET /api/admin/attributes
     - Returns every auto-inferred targeting attribute in the project — the `name` and (when known) the value `type` — for building gate/experiment targeting rules.  **Use case:** Discover which user-context keys are available before authoring a targeting rule, instead of guessing attribute names.
     - Bearer Token:
       - type: http
       - name: bearerSdkKey
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - parameter q: (query) Case-insensitive substring filter across the resource&#39;s human-readable text columns (e.g. &#x60;name&#x60;, &#x60;title&#x60;, &#x60;description&#x60;). OR-matched across those columns; omit to return everything. (optional)
     - returns: RequestBuilder<[ListAttributesResponseInner]> 
     */
    open class func listAttributesWithRequestBuilder(xProjectId: String? = nil, q: String? = nil) -> RequestBuilder<[ListAttributesResponseInner]> {
        let localVariablePath = "/api/admin/attributes"
        let localVariableURLString = ShipeasyAdminAPI.basePath + localVariablePath
        let localVariableParameters: [String: Any]? = nil

        var localVariableUrlComponents = URLComponents(string: localVariableURLString)
        localVariableUrlComponents?.queryItems = APIHelper.mapValuesToQueryItems([
            "q": (wrappedValue: q?.encodeToJSON(), isExplode: true),
        ])

        let localVariableNillableHeaders: [String: Any?] = [
            "X-Project-Id": xProjectId?.encodeToJSON(),
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<[ListAttributesResponseInner]>.Type = ShipeasyAdminAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "GET", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }

    /**
     Update a targeting attribute
     
     - parameter id: (path) The attribute id. 
     - parameter updateAttributeRequest: (body)  
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - returns: UpdateAttributeResponse
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func updateAttribute(id: String, updateAttributeRequest: UpdateAttributeRequest, xProjectId: String? = nil) async throws -> UpdateAttributeResponse {
        return try await updateAttributeWithRequestBuilder(id: id, updateAttributeRequest: updateAttributeRequest, xProjectId: xProjectId).execute().body
    }

    /**
     Update a targeting attribute
     - PATCH /api/admin/attributes/{id}
     - Update a targeting attribute's type, allowed values, required flag, description, or SDK path. `name` is immutable.  **Use case:** Add an allowed value to an `enum` attribute, or flip its required flag.
     - Bearer Token:
       - type: http
       - name: bearerSdkKey
     - parameter id: (path) The attribute id. 
     - parameter updateAttributeRequest: (body)  
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - returns: RequestBuilder<UpdateAttributeResponse> 
     */
    open class func updateAttributeWithRequestBuilder(id: String, updateAttributeRequest: UpdateAttributeRequest, xProjectId: String? = nil) -> RequestBuilder<UpdateAttributeResponse> {
        var localVariablePath = "/api/admin/attributes/{id}"
        let idPreEscape = "\(APIHelper.mapValueToPathItem(id))"
        let idPostEscape = idPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        localVariablePath = localVariablePath.replacingOccurrences(of: "{id}", with: idPostEscape, options: .literal, range: nil)
        let localVariableURLString = ShipeasyAdminAPI.basePath + localVariablePath
        let localVariableParameters = JSONEncodingHelper.encodingParameters(forEncodableObject: updateAttributeRequest)

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            "Content-Type": "application/json",
            "X-Project-Id": xProjectId?.encodeToJSON(),
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<UpdateAttributeResponse>.Type = ShipeasyAdminAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "PATCH", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }
}
