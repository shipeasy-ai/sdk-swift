//
// DraftsAPI.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

open class DraftsAPI {

    /**
     Create a translation draft
     
     - parameter createI18nDraftRequest: (body)  
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - returns: I18nDraft
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func createI18nDraft(createI18nDraftRequest: CreateI18nDraftRequest, xProjectId: String? = nil) async throws -> I18nDraft {
        return try await createI18nDraftWithRequestBuilder(createI18nDraftRequest: createI18nDraftRequest, xProjectId: xProjectId).execute().body
    }

    /**
     Create a translation draft
     - POST /api/admin/i18n/drafts
     - Stage a new translation draft against a target profile, optionally seeding its keys from a source profile.  **Use case:** Open a machine-translation draft for review before publishing it to a locale.
     - Bearer Token:
       - type: http
       - name: bearerSdkKey
     - parameter createI18nDraftRequest: (body)  
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - returns: RequestBuilder<I18nDraft> 
     */
    open class func createI18nDraftWithRequestBuilder(createI18nDraftRequest: CreateI18nDraftRequest, xProjectId: String? = nil) -> RequestBuilder<I18nDraft> {
        let localVariablePath = "/api/admin/i18n/drafts"
        let localVariableURLString = ShipeasyAdminAPI.basePath + localVariablePath
        let localVariableParameters = JSONEncodingHelper.encodingParameters(forEncodableObject: createI18nDraftRequest)

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            "Content-Type": "application/json",
            "X-Project-Id": xProjectId?.encodeToJSON(),
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<I18nDraft>.Type = ShipeasyAdminAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "POST", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }

    /**
     List translation drafts
     
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - returns: [ListI18nDraftsResponseInner]
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func listI18nDrafts(xProjectId: String? = nil) async throws -> [ListI18nDraftsResponseInner] {
        return try await listI18nDraftsWithRequestBuilder(xProjectId: xProjectId).execute().body
    }

    /**
     List translation drafts
     - GET /api/admin/i18n/drafts
     - List staged translation drafts awaiting review/publish.  **Use case:** Review machine-translation drafts before publishing them to a locale.
     - Bearer Token:
       - type: http
       - name: bearerSdkKey
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - returns: RequestBuilder<[ListI18nDraftsResponseInner]> 
     */
    open class func listI18nDraftsWithRequestBuilder(xProjectId: String? = nil) -> RequestBuilder<[ListI18nDraftsResponseInner]> {
        let localVariablePath = "/api/admin/i18n/drafts"
        let localVariableURLString = ShipeasyAdminAPI.basePath + localVariablePath
        let localVariableParameters: [String: Any]? = nil

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            "X-Project-Id": xProjectId?.encodeToJSON(),
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<[ListI18nDraftsResponseInner]>.Type = ShipeasyAdminAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "GET", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }

    /**
     Update a translation draft
     
     - parameter draftId: (path) The draft id to update. 
     - parameter updateI18nDraftRequest: (body)  
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - returns: I18nDraft
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func updateI18nDraft(draftId: String, updateI18nDraftRequest: UpdateI18nDraftRequest, xProjectId: String? = nil) async throws -> I18nDraft {
        return try await updateI18nDraftWithRequestBuilder(draftId: draftId, updateI18nDraftRequest: updateI18nDraftRequest, xProjectId: xProjectId).execute().body
    }

    /**
     Update a translation draft
     - PATCH /api/admin/i18n/drafts/{draftId}
     - Transition a draft's lifecycle state (`open` / `merged` / `abandoned`).  **Use case:** Mark a reviewed draft as merged, or abandon one that should not ship.
     - Bearer Token:
       - type: http
       - name: bearerSdkKey
     - parameter draftId: (path) The draft id to update. 
     - parameter updateI18nDraftRequest: (body)  
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - returns: RequestBuilder<I18nDraft> 
     */
    open class func updateI18nDraftWithRequestBuilder(draftId: String, updateI18nDraftRequest: UpdateI18nDraftRequest, xProjectId: String? = nil) -> RequestBuilder<I18nDraft> {
        var localVariablePath = "/api/admin/i18n/drafts/{draftId}"
        let draftIdPreEscape = "\(APIHelper.mapValueToPathItem(draftId))"
        let draftIdPostEscape = draftIdPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        localVariablePath = localVariablePath.replacingOccurrences(of: "{draftId}", with: draftIdPostEscape, options: .literal, range: nil)
        let localVariableURLString = ShipeasyAdminAPI.basePath + localVariablePath
        let localVariableParameters = JSONEncodingHelper.encodingParameters(forEncodableObject: updateI18nDraftRequest)

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            "Content-Type": "application/json",
            "X-Project-Id": xProjectId?.encodeToJSON(),
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<I18nDraft>.Type = ShipeasyAdminAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "PATCH", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }
}
