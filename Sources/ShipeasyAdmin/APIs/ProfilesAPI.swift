//
// ProfilesAPI.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

open class ProfilesAPI {

    /**
     Create an i18n profile
     
     - parameter createI18nProfileRequest: (body)  
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - returns: CreateI18nProfileResponse
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func createI18nProfile(createI18nProfileRequest: CreateI18nProfileRequest, xProjectId: String? = nil) async throws -> CreateI18nProfileResponse {
        return try await createI18nProfileWithRequestBuilder(createI18nProfileRequest: createI18nProfileRequest, xProjectId: xProjectId).execute().body
    }

    /**
     Create an i18n profile
     - POST /api/admin/i18n/profiles
     - Create a locale profile. `name` is the stable handle (e.g. `fr:prod`).  **Use case:** Stand up a new locale before seeding its keys.
     - Bearer Token:
       - type: http
       - name: bearerSdkKey
     - parameter createI18nProfileRequest: (body)  
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - returns: RequestBuilder<CreateI18nProfileResponse> 
     */
    open class func createI18nProfileWithRequestBuilder(createI18nProfileRequest: CreateI18nProfileRequest, xProjectId: String? = nil) -> RequestBuilder<CreateI18nProfileResponse> {
        let localVariablePath = "/api/admin/i18n/profiles"
        let localVariableURLString = ShipeasyAdminAPI.basePath + localVariablePath
        let localVariableParameters = JSONEncodingHelper.encodingParameters(forEncodableObject: createI18nProfileRequest)

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            "Content-Type": "application/json",
            "X-Project-Id": xProjectId?.encodeToJSON(),
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<CreateI18nProfileResponse>.Type = ShipeasyAdminAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "POST", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }

    /**
     List i18n profiles
     
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - returns: [ListI18nProfilesResponseInner]
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func listI18nProfiles(xProjectId: String? = nil) async throws -> [ListI18nProfilesResponseInner] {
        return try await listI18nProfilesWithRequestBuilder(xProjectId: xProjectId).execute().body
    }

    /**
     List i18n profiles
     - GET /api/admin/i18n/profiles
     - Returns every locale profile in the project (e.g. `en:prod`, `fr:prod`).  **Use case:** Discover which locale profiles exist before pushing keys or publishing a chunk.
     - Bearer Token:
       - type: http
       - name: bearerSdkKey
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - returns: RequestBuilder<[ListI18nProfilesResponseInner]> 
     */
    open class func listI18nProfilesWithRequestBuilder(xProjectId: String? = nil) -> RequestBuilder<[ListI18nProfilesResponseInner]> {
        let localVariablePath = "/api/admin/i18n/profiles"
        let localVariableURLString = ShipeasyAdminAPI.basePath + localVariablePath
        let localVariableParameters: [String: Any]? = nil

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            "X-Project-Id": xProjectId?.encodeToJSON(),
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<[ListI18nProfilesResponseInner]>.Type = ShipeasyAdminAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "GET", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }

    /**
     Publish a profile live
     
     - parameter profileId: (path) The profile id to publish. 
     - parameter publishI18nProfileRequest: (body)  
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - returns: PublishI18nProfileResponse
     */
    @available(macOS 10.15, iOS 13.0, tvOS 13.0, watchOS 6.0, *)
    open class func publishI18nProfile(profileId: String, publishI18nProfileRequest: PublishI18nProfileRequest, xProjectId: String? = nil) async throws -> PublishI18nProfileResponse {
        return try await publishI18nProfileWithRequestBuilder(profileId: profileId, publishI18nProfileRequest: publishI18nProfileRequest, xProjectId: xProjectId).execute().body
    }

    /**
     Publish a profile live
     - POST /api/admin/i18n/profiles/{profileId}/publish
     - Publish a profile to the CDN — rebuild its KV snapshot + purge the edge. Publishing is PROFILE-WIDE: the whole profile is snapshotted into one KV blob, so the optional `chunk` in the body is an audit label only (it does not scope what ships).  **Use case:** Ship the latest translations live after pushing/updating keys.
     - Bearer Token:
       - type: http
       - name: bearerSdkKey
     - parameter profileId: (path) The profile id to publish. 
     - parameter publishI18nProfileRequest: (body)  
     - parameter xProjectId: (header) Project the request operates on. Optional — defaults to the project the SDK key belongs to; pass it only to scope a multi-project key (the generated client sets it once from its configuration, so per-call callers never thread it). (optional)
     - returns: RequestBuilder<PublishI18nProfileResponse> 
     */
    open class func publishI18nProfileWithRequestBuilder(profileId: String, publishI18nProfileRequest: PublishI18nProfileRequest, xProjectId: String? = nil) -> RequestBuilder<PublishI18nProfileResponse> {
        var localVariablePath = "/api/admin/i18n/profiles/{profileId}/publish"
        let profileIdPreEscape = "\(APIHelper.mapValueToPathItem(profileId))"
        let profileIdPostEscape = profileIdPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        localVariablePath = localVariablePath.replacingOccurrences(of: "{profileId}", with: profileIdPostEscape, options: .literal, range: nil)
        let localVariableURLString = ShipeasyAdminAPI.basePath + localVariablePath
        let localVariableParameters = JSONEncodingHelper.encodingParameters(forEncodableObject: publishI18nProfileRequest)

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: Any?] = [
            "Content-Type": "application/json",
            "X-Project-Id": xProjectId?.encodeToJSON(),
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<PublishI18nProfileResponse>.Type = ShipeasyAdminAPI.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "POST", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true)
    }
}
