//
// ListAlertRulesResponseInner.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** One metric-threshold alert rule. */
public struct ListAlertRulesResponseInner: Codable, JSONEncodable {

    public enum Comparator: String, Codable, CaseIterable {
        case gt = "gt"
        case gte = "gte"
        case lt = "lt"
        case lte = "lte"
    }
    public enum Severity: String, Codable, CaseIterable {
        case danger = "danger"
        case warn = "warn"
        case info = "info"
    }
    public static let windowHoursRule = NumericRule<Int>(minimum: -9007199254740991, exclusiveMinimum: false, maximum: 9007199254740991, exclusiveMaximum: false, multipleOf: nil)
    /** Stable opaque alert-rule id. */
    public var id: String
    /** Human label for the rule. */
    public var name: String
    /** Id of the metric the rule evaluates. Immutable after create. */
    public var metricId: String
    /** Display name of the bound metric, or `null` if the metric no longer exists. */
    public var metricName: String?
    /** How the metric value is compared to the threshold (gt/gte/lt/lte). */
    public var comparator: Comparator
    /** Threshold the metric value is compared against. */
    public var threshold: Double
    /** Lookback window (hours) the metric is aggregated over. */
    public var windowHours: Int
    /** Severity of the raised alert. */
    public var severity: Severity
    /** Whether the rule is evaluated by the cron. */
    public var enabled: Bool
    public var notify: NotificationTarget?
    /** ISO-8601 timestamp of creation. */
    public var createdAt: String
    /** ISO-8601 timestamp of the last update. */
    public var updatedAt: String

    public init(id: String, name: String, metricId: String, metricName: String?, comparator: Comparator, threshold: Double, windowHours: Int, severity: Severity, enabled: Bool, notify: NotificationTarget?, createdAt: String, updatedAt: String) {
        self.id = id
        self.name = name
        self.metricId = metricId
        self.metricName = metricName
        self.comparator = comparator
        self.threshold = threshold
        self.windowHours = windowHours
        self.severity = severity
        self.enabled = enabled
        self.notify = notify
        self.createdAt = createdAt
        self.updatedAt = updatedAt
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case id
        case name
        case metricId
        case metricName
        case comparator
        case threshold
        case windowHours
        case severity
        case enabled
        case notify
        case createdAt
        case updatedAt
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(id, forKey: .id)
        try container.encode(name, forKey: .name)
        try container.encode(metricId, forKey: .metricId)
        try container.encode(metricName, forKey: .metricName)
        try container.encode(comparator, forKey: .comparator)
        try container.encode(threshold, forKey: .threshold)
        try container.encode(windowHours, forKey: .windowHours)
        try container.encode(severity, forKey: .severity)
        try container.encode(enabled, forKey: .enabled)
        try container.encode(notify, forKey: .notify)
        try container.encode(createdAt, forKey: .createdAt)
        try container.encode(updatedAt, forKey: .updatedAt)
    }
}


