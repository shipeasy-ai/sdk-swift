//
// OpsItemRelated.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Deep links to the resources behind this item — every value is either a dashboard-relative path or an admin-API path fetchable with the same key. */
public struct OpsItemRelated: Codable, JSONEncodable {

    /** Dashboard-relative deep link to this item. */
    public var dashboard: String?
    /** Admin-API path to the tracked error (error tickets only). */
    public var error: String?
    /** Admin-API path to the alert rule (metric-rule alerts only). */
    public var alertRule: String?
    /** Admin-API path to the metric the alert watches (alert tickets only). */
    public var metric: String?
    /** HTML URL of the connected GitHub issue, if any. */
    public var githubIssue: String?
    /** HTML URL of the linked GitHub pull request, if any. */
    public var githubPr: String?

    public init(dashboard: String? = nil, error: String? = nil, alertRule: String? = nil, metric: String? = nil, githubIssue: String? = nil, githubPr: String? = nil) {
        self.dashboard = dashboard
        self.error = error
        self.alertRule = alertRule
        self.metric = metric
        self.githubIssue = githubIssue
        self.githubPr = githubPr
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case dashboard
        case error
        case alertRule
        case metric
        case githubIssue
        case githubPr
    }

    public var additionalProperties: [String: AnyCodable] = [:]

    public subscript(key: String) -> AnyCodable? {
        get {
            if let value = additionalProperties[key] {
                return value
            }
            return nil
        }

        set {
            additionalProperties[key] = newValue
        }
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(dashboard, forKey: .dashboard)
        try container.encodeIfPresent(error, forKey: .error)
        try container.encodeIfPresent(alertRule, forKey: .alertRule)
        try container.encodeIfPresent(metric, forKey: .metric)
        try container.encodeIfPresent(githubIssue, forKey: .githubIssue)
        try container.encodeIfPresent(githubPr, forKey: .githubPr)
        var additionalPropertiesContainer = encoder.container(keyedBy: String.self)
        try additionalPropertiesContainer.encodeMap(additionalProperties)
    }

    // Decodable protocol methods

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        dashboard = try container.decodeIfPresent(String.self, forKey: .dashboard)
        error = try container.decodeIfPresent(String.self, forKey: .error)
        alertRule = try container.decodeIfPresent(String.self, forKey: .alertRule)
        metric = try container.decodeIfPresent(String.self, forKey: .metric)
        githubIssue = try container.decodeIfPresent(String.self, forKey: .githubIssue)
        githubPr = try container.decodeIfPresent(String.self, forKey: .githubPr)
        var nonAdditionalPropertyKeys = Set<String>()
        nonAdditionalPropertyKeys.insert("dashboard")
        nonAdditionalPropertyKeys.insert("error")
        nonAdditionalPropertyKeys.insert("alertRule")
        nonAdditionalPropertyKeys.insert("metric")
        nonAdditionalPropertyKeys.insert("githubIssue")
        nonAdditionalPropertyKeys.insert("githubPr")
        let additionalPropertiesContainer = try decoder.container(keyedBy: String.self)
        additionalProperties = try additionalPropertiesContainer.decodeMap(AnyCodable.self, excludedKeys: nonAdditionalPropertyKeys)
    }
}

