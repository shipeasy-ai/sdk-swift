//
// NotifyOpsResponse.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Response for &#x60;POST /api/admin/notifications&#x60;. */
public struct NotifyOpsResponse: Codable, JSONEncodable {

    /** The dedupe key the escalation was recorded under. */
    public var dedupeKey: String
    /** `true` if a new escalation was dispatched; `false` on an idempotent repeat. */
    public var dispatched: Bool

    public init(dedupeKey: String, dispatched: Bool) {
        self.dedupeKey = dedupeKey
        self.dispatched = dispatched
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case dedupeKey
        case dispatched
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(dedupeKey, forKey: .dedupeKey)
        try container.encode(dispatched, forKey: .dispatched)
    }
}

