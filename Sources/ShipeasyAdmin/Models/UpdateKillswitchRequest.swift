//
// UpdateKillswitchRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Body for &#x60;PATCH /api/admin/killswitches/{id}&#x60;. Partial — only supplied fields change. &#x60;switches&#x60; replaces wholesale (no merge). */
public struct UpdateKillswitchRequest: Codable, JSONEncodable {

    public static let descriptionRule = StringRule(minLength: nil, maxLength: 512, pattern: nil)
    public static let folderRule = StringRule(minLength: nil, maxLength: 256, pattern: "/^[a-zA-Z0-9_-]+$/")
    /** New description, or `null` to clear it. Max 512 chars. */
    public var description: String?
    /** Optional folder name grouping items in the dashboard. Alphanumeric, `_` or `-` (no `/`). Part of the SDK lookup key (`<folder>/<name>`). */
    public var folder: String?
    /** Flat value applied to every env. Publishes a new version per env when set. Omit to leave values unchanged. */
    public var value: Bool?
    /** Replace the switches map wholesale on every env. To edit a single entry on a single env use `PUT /{id}/switch` instead. */
    public var switches: [String: Bool]?

    public init(description: String? = nil, folder: String? = nil, value: Bool? = nil, switches: [String: Bool]? = nil) {
        self.description = description
        self.folder = folder
        self.value = value
        self.switches = switches
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case description
        case folder
        case value
        case switches
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(description, forKey: .description)
        try container.encodeIfPresent(folder, forKey: .folder)
        try container.encodeIfPresent(value, forKey: .value)
        try container.encodeIfPresent(switches, forKey: .switches)
    }
}

