//
// ListGatesResponseDataInnerStackInnerOneOf1.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Percentage-rollout sub-gate. Hashes &#x60;bucketBy&#x60; (default &#x60;userID&#x60;) with &#x60;salt&#x60; and returns &#x60;true&#x60; for &#x60;rolloutPct/10000&#x60; fraction of callers (or the ramp&#39;s effective % when &#x60;ramp&#x60; is set). */
public struct ListGatesResponseDataInnerStackInnerOneOf1: Codable, JSONEncodable {

    public enum ModelType: String, Codable, CaseIterable {
        case rollout = "rollout"
    }
    public static let idRule = StringRule(minLength: 1, maxLength: nil, pattern: nil)
    public static let rolloutPctRule = NumericRule<Int>(minimum: 0, exclusiveMinimum: false, maximum: 10000, exclusiveMaximum: false, multipleOf: nil)
    /** Client-supplied stable id for the stack entry. */
    public var id: String
    /** Discriminator. `rollout` = percentage-bucket sub-gate. */
    public var type: ModelType
    /** Human label shown in the dashboard. */
    public var name: String?
    /** Source template id, or `null`. */
    public var fromTemplate: String?
    /** Bucket size in basis points (0–10000 = 0%–100%). `5000` = 50%, `100` = 1%, allows sub-1% precision. */
    public var rolloutPct: Int
    /** Attribute used to hash the caller into a bucket. Defaults to `user.userID`. Use e.g. `accountID` to bucket whole accounts together. */
    public var bucketBy: String?
    /** Per-entry hash salt. Defaults to the gate's salt. Set explicitly to keep two rollouts independent or correlated across gates. */
    public var salt: String?
    public var ramp: ListGatesResponseDataInnerStackInnerOneOfRamp?
    /** If `true`, dashboard hides edit controls. */
    public var locked: Bool?

    public init(id: String, type: ModelType, name: String? = nil, fromTemplate: String? = nil, rolloutPct: Int, bucketBy: String? = nil, salt: String? = nil, ramp: ListGatesResponseDataInnerStackInnerOneOfRamp? = nil, locked: Bool? = nil) {
        self.id = id
        self.type = type
        self.name = name
        self.fromTemplate = fromTemplate
        self.rolloutPct = rolloutPct
        self.bucketBy = bucketBy
        self.salt = salt
        self.ramp = ramp
        self.locked = locked
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case id
        case type
        case name
        case fromTemplate
        case rolloutPct
        case bucketBy
        case salt
        case ramp
        case locked
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(id, forKey: .id)
        try container.encode(type, forKey: .type)
        try container.encodeIfPresent(name, forKey: .name)
        try container.encodeIfPresent(fromTemplate, forKey: .fromTemplate)
        try container.encode(rolloutPct, forKey: .rolloutPct)
        try container.encodeIfPresent(bucketBy, forKey: .bucketBy)
        try container.encodeIfPresent(salt, forKey: .salt)
        try container.encodeIfPresent(ramp, forKey: .ramp)
        try container.encodeIfPresent(locked, forKey: .locked)
    }
}


@available(iOS 13, tvOS 13, watchOS 6, macOS 10.15, *)
extension ListGatesResponseDataInnerStackInnerOneOf1: Identifiable {}
