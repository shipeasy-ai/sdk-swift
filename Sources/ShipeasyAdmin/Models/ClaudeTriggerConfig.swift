//
// ClaudeTriggerConfig.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Non-secret config for a Claude trigger. */
public struct ClaudeTriggerConfig: Codable, JSONEncodable {

    public static let routineIdRule = StringRule(minLength: 1, maxLength: nil, pattern: nil)
    public static let fireTextRule = StringRule(minLength: 1, maxLength: nil, pattern: nil)
    /** The Claude Code routine id this connector fires (the id recorded at setup). Idempotency key for the connector. */
    public var routineId: String
    /** Optional default prompt sent on a manual fire / when no event text applies. */
    public var fireText: String?

    public init(routineId: String, fireText: String? = nil) {
        self.routineId = routineId
        self.fireText = fireText
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case routineId
        case fireText
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(routineId, forKey: .routineId)
        try container.encodeIfPresent(fireText, forKey: .fireText)
    }
}

