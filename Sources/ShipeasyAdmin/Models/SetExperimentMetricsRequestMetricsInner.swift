//
// SetExperimentMetricsRequestMetricsInner.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct SetExperimentMetricsRequestMetricsInner: Codable, JSONEncodable {

    public enum Role: String, Codable, CaseIterable {
        case goal = "goal"
        case guardrail = "guardrail"
        case secondary = "secondary"
    }
    /** Existing metric id in the project. */
    public var metricId: String
    /** Metric role. `goal` drives the decision, `guardrail` blocks ship if degraded, `secondary` is informational. */
    public var role: Role
    /** Per-experiment override of the metric's `default_min_effect_of_interest` (relative, 0–1) — the smallest change worth acting on for THIS experiment's decision, which depends on the intervention's cost/risk. `null`/omitted falls back to the metric default. For a `guardrail` this is the non-inferiority margin (how large a regression is tolerated); for a `goal` it is the superiority threshold. */
    public var minEffectOfInterest: Double?

    public init(metricId: String, role: Role, minEffectOfInterest: Double? = nil) {
        self.metricId = metricId
        self.role = role
        self.minEffectOfInterest = minEffectOfInterest
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case metricId = "metric_id"
        case role
        case minEffectOfInterest = "min_effect_of_interest"
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(metricId, forKey: .metricId)
        try container.encode(role, forKey: .role)
        try container.encodeIfPresent(minEffectOfInterest, forKey: .minEffectOfInterest)
    }
}

