//
// UpdateErrorStatusRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Body for &#x60;PATCH /api/admin/errors/{id}&#x60;. The only mutable field is &#x60;status&#x60; (open ⇄ resolved ⇄ ignored). */
public struct UpdateErrorStatusRequest: Codable, JSONEncodable {

    public enum Status: String, Codable, CaseIterable {
        case _open = "open"
        case resolved = "resolved"
        case ignored = "ignored"
    }
    /** New triage state. `resolved` reopens automatically on recurrence; `ignored` is sticky. */
    public var status: Status

    public init(status: Status) {
        self.status = status
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case status
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(status, forKey: .status)
    }
}

