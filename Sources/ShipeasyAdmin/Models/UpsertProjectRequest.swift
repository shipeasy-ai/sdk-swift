//
// UpsertProjectRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Body for &#x60;POST /api/admin/projects/upsert&#x60;. Only &#x60;domain&#x60; is required; the owner is resolved from the caller&#39;s session. */
public struct UpsertProjectRequest: Codable, JSONEncodable {

    public static let domainRule = StringRule(minLength: 1, maxLength: 2048, pattern: "/^(\\*|(\\*\\.)?[a-z0-9]([a-z0-9-]{0,61}[a-z0-9])?(\\.[a-z0-9]([a-z0-9-]{0,61}[a-z0-9])?)+)$/")
    public static let nameRule = StringRule(minLength: 1, maxLength: 100, pattern: nil)
    /** Lowercase bare hostname (e.g. `acme.com`, `app.acme.com`, `*.acme.com`), or `*` to allow any origin. Full URLs with `https://` are not accepted. The project is keyed by `(owner_email, domain)`, so a second call with the same domain returns the existing project. */
    public var domain: String
    /** Human-readable project name. Defaults to the domain on first create. */
    public var name: String?

    public init(domain: String, name: String? = nil) {
        self.domain = domain
        self.name = name
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case domain
        case name
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(domain, forKey: .domain)
        try container.encodeIfPresent(name, forKey: .name)
    }
}

