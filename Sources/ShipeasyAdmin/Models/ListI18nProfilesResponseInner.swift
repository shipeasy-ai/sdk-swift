//
// ListI18nProfilesResponseInner.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** One i18n locale profile. */
public struct ListI18nProfilesResponseInner: Codable, JSONEncodable {

    /** Stable opaque profile id. */
    public var id: String
    /** Profile handle, e.g. `en:prod` or `fr:prod`. */
    public var name: String
    /** `1` for the project's single default profile (always seeded as `en:prod`), else `0`. */
    public var isDefault: Double?
    /** ISO-8601 timestamp of creation. */
    public var createdAt: String?
    /** ISO-8601 soft-delete timestamp, or `null` if live. */
    public var deletedAt: String?

    public init(id: String, name: String, isDefault: Double? = nil, createdAt: String? = nil, deletedAt: String? = nil) {
        self.id = id
        self.name = name
        self.isDefault = isDefault
        self.createdAt = createdAt
        self.deletedAt = deletedAt
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case id
        case name
        case isDefault
        case createdAt
        case deletedAt
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(id, forKey: .id)
        try container.encode(name, forKey: .name)
        try container.encodeIfPresent(isDefault, forKey: .isDefault)
        try container.encodeIfPresent(createdAt, forKey: .createdAt)
        try container.encodeIfPresent(deletedAt, forKey: .deletedAt)
    }
}


