//
// QueryIrGroupBy.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Optional group-by clause (ignored for experiment analysis). */
public struct QueryIrGroupBy: Codable, JSONEncodable {

    public enum Op: String, Codable, CaseIterable {
        case by = "by"
        case without = "without"
    }
    public static let labelsRule = ArrayRule(minItems: nil, maxItems: 5, uniqueItems: false)
    /** `by` keeps the listed labels; `without` drops them. */
    public var op: Op
    /** Labels to group by (max 5). */
    public var labels: [String]

    public init(op: Op, labels: [String]) {
        self.op = op
        self.labels = labels
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case op
        case labels
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(op, forKey: .op)
        try container.encode(labels, forKey: .labels)
    }
}

