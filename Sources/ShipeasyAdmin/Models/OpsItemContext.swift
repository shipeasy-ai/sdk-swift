//
// OpsItemContext.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Per-type capture context on a queue item. Exactly one of &#x60;browser&#x60; (bug/feature), &#x60;error&#x60;, &#x60;alert&#x60;, or &#x60;measurePlan&#x60; is populated depending on &#x60;type&#x60;; other keys (e.g. the AI dedupe block on bugs) may also be present. */
public struct OpsItemContext: Codable, JSONEncodable {

    public var browser: OpsBrowserContext?
    public var error: OpsErrorContext?
    public var alert: OpsAlertContext?
    public var measurePlan: OpsMeasurePlanContext?

    public init(browser: OpsBrowserContext? = nil, error: OpsErrorContext? = nil, alert: OpsAlertContext? = nil, measurePlan: OpsMeasurePlanContext? = nil) {
        self.browser = browser
        self.error = error
        self.alert = alert
        self.measurePlan = measurePlan
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case browser
        case error
        case alert
        case measurePlan
    }

    public var additionalProperties: [String: AnyCodable] = [:]

    public subscript(key: String) -> AnyCodable? {
        get {
            if let value = additionalProperties[key] {
                return value
            }
            return nil
        }

        set {
            additionalProperties[key] = newValue
        }
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(browser, forKey: .browser)
        try container.encodeIfPresent(error, forKey: .error)
        try container.encodeIfPresent(alert, forKey: .alert)
        try container.encodeIfPresent(measurePlan, forKey: .measurePlan)
        var additionalPropertiesContainer = encoder.container(keyedBy: String.self)
        try additionalPropertiesContainer.encodeMap(additionalProperties)
    }

    // Decodable protocol methods

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        browser = try container.decodeIfPresent(OpsBrowserContext.self, forKey: .browser)
        error = try container.decodeIfPresent(OpsErrorContext.self, forKey: .error)
        alert = try container.decodeIfPresent(OpsAlertContext.self, forKey: .alert)
        measurePlan = try container.decodeIfPresent(OpsMeasurePlanContext.self, forKey: .measurePlan)
        var nonAdditionalPropertyKeys = Set<String>()
        nonAdditionalPropertyKeys.insert("browser")
        nonAdditionalPropertyKeys.insert("error")
        nonAdditionalPropertyKeys.insert("alert")
        nonAdditionalPropertyKeys.insert("measurePlan")
        let additionalPropertiesContainer = try decoder.container(keyedBy: String.self)
        additionalProperties = try additionalPropertiesContainer.decodeMap(AnyCodable.self, excludedKeys: nonAdditionalPropertyKeys)
    }
}

