//
// CreateAlertRuleRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct CreateAlertRuleRequest: Codable, JSONEncodable {

    public enum Comparator: String, Codable, CaseIterable {
        case gt = "gt"
        case gte = "gte"
        case lt = "lt"
        case lte = "lte"
    }
    public enum Severity: String, Codable, CaseIterable {
        case danger = "danger"
        case warn = "warn"
        case info = "info"
    }
    public static let nameRule = StringRule(minLength: 1, maxLength: 120, pattern: nil)
    public static let metricIdRule = StringRule(minLength: 1, maxLength: nil, pattern: nil)
    public static let windowHoursRule = NumericRule<Int>(minimum: 1, exclusiveMinimum: false, maximum: 720, exclusiveMaximum: false, multipleOf: nil)
    /** Human label for the rule, shown on the alert and the rules list. */
    public var name: String
    /** Id of the metric to evaluate. */
    public var metricId: String
    /** How the metric value is compared to the threshold (gt/gte/lt/lte). */
    public var comparator: Comparator
    /** Threshold the metric value is compared against. */
    public var threshold: Double
    /** Lookback window (hours) the metric is aggregated over. 1–720. */
    public var windowHours: Int? = 24
    /** Severity of the raised alert. */
    public var severity: Severity? = .warn
    /** Whether the rule is evaluated by the cron. */
    public var enabled: Bool? = true
    public var notify: NotificationTarget?

    public init(name: String, metricId: String, comparator: Comparator, threshold: Double, windowHours: Int? = 24, severity: Severity? = .warn, enabled: Bool? = true, notify: NotificationTarget? = nil) {
        self.name = name
        self.metricId = metricId
        self.comparator = comparator
        self.threshold = threshold
        self.windowHours = windowHours
        self.severity = severity
        self.enabled = enabled
        self.notify = notify
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case name
        case metricId
        case comparator
        case threshold
        case windowHours
        case severity
        case enabled
        case notify
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(name, forKey: .name)
        try container.encode(metricId, forKey: .metricId)
        try container.encode(comparator, forKey: .comparator)
        try container.encode(threshold, forKey: .threshold)
        try container.encodeIfPresent(windowHours, forKey: .windowHours)
        try container.encodeIfPresent(severity, forKey: .severity)
        try container.encodeIfPresent(enabled, forKey: .enabled)
        try container.encodeIfPresent(notify, forKey: .notify)
    }
}

