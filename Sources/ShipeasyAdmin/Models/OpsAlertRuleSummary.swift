//
// OpsAlertRuleSummary.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** The condition of a metric-rule alert. */
public struct OpsAlertRuleSummary: Codable, JSONEncodable {

    public enum Comparator: String, Codable, CaseIterable {
        case gt = "gt"
        case gte = "gte"
        case lt = "lt"
        case lte = "lte"
    }
    /** Alert-rule id. */
    public var id: String
    /** Alert-rule name. */
    public var name: String
    /** How `observedValue` is compared to `threshold`. */
    public var comparator: Comparator
    /** The threshold value the rule fires on. */
    public var threshold: Double
    /** Rolling evaluation window in hours. */
    public var windowHours: Double

    public init(id: String, name: String, comparator: Comparator, threshold: Double, windowHours: Double) {
        self.id = id
        self.name = name
        self.comparator = comparator
        self.threshold = threshold
        self.windowHours = windowHours
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case id
        case name
        case comparator
        case threshold
        case windowHours
    }

    public var additionalProperties: [String: AnyCodable] = [:]

    public subscript(key: String) -> AnyCodable? {
        get {
            if let value = additionalProperties[key] {
                return value
            }
            return nil
        }

        set {
            additionalProperties[key] = newValue
        }
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(id, forKey: .id)
        try container.encode(name, forKey: .name)
        try container.encode(comparator, forKey: .comparator)
        try container.encode(threshold, forKey: .threshold)
        try container.encode(windowHours, forKey: .windowHours)
        var additionalPropertiesContainer = encoder.container(keyedBy: String.self)
        try additionalPropertiesContainer.encodeMap(additionalProperties)
    }

    // Decodable protocol methods

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        id = try container.decode(String.self, forKey: .id)
        name = try container.decode(String.self, forKey: .name)
        comparator = try container.decode(Comparator.self, forKey: .comparator)
        threshold = try container.decode(Double.self, forKey: .threshold)
        windowHours = try container.decode(Double.self, forKey: .windowHours)
        var nonAdditionalPropertyKeys = Set<String>()
        nonAdditionalPropertyKeys.insert("id")
        nonAdditionalPropertyKeys.insert("name")
        nonAdditionalPropertyKeys.insert("comparator")
        nonAdditionalPropertyKeys.insert("threshold")
        nonAdditionalPropertyKeys.insert("windowHours")
        let additionalPropertiesContainer = try decoder.container(keyedBy: String.self)
        additionalProperties = try additionalPropertiesContainer.decodeMap(AnyCodable.self, excludedKeys: nonAdditionalPropertyKeys)
    }
}


