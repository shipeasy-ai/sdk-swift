//
// CreateOAuthConnectorRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Create an OAuth/app connector stub. The connector is created &#x60;enabled: false&#x60; with empty &#x60;config&#x60; and no credentials; the provider&#39;s OAuth flow then attaches credentials and enables it. */
public struct CreateOAuthConnectorRequest: Codable, JSONEncodable {

    public enum Provider: String, Codable, CaseIterable {
        case googleSheets = "google_sheets"
        case github = "github"
        case slack = "slack"
    }
    public static let nameRule = StringRule(minLength: 1, maxLength: 80, pattern: nil)
    public static let eventsRule = ArrayRule(minItems: 1, maxItems: nil, uniqueItems: false)
    /** OAuth/app connector destination. */
    public var provider: Provider
    /** Human-readable connector label. */
    public var name: String
    /** Events to subscribe to. At least one is required. */
    public var events: [ConnectorEvent]

    public init(provider: Provider, name: String, events: [ConnectorEvent]) {
        self.provider = provider
        self.name = name
        self.events = events
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case provider
        case name
        case events
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(provider, forKey: .provider)
        try container.encode(name, forKey: .name)
        try container.encode(events, forKey: .events)
    }
}

