//
// ListEventsResponseInner.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** A catalogued event. */
public struct ListEventsResponseInner: Codable, JSONEncodable {

    public static let folderRule = StringRule(minLength: nil, maxLength: 256, pattern: "/^[a-zA-Z0-9_-]+$/")
    public static let pendingRule = NumericRule<Int>(minimum: -9007199254740991, exclusiveMinimum: false, maximum: 9007199254740991, exclusiveMaximum: false, multipleOf: nil)
    /** Stable opaque event id. */
    public var id: String
    /** Event name — the handle metric queries reference. */
    public var name: String
    /** Optional folder name used to group items in the dashboard. Part of the SDK lookup key: an item in folder `checkout` named `new-cart` is referenced as `checkout/new-cart` from the SDK. */
    public var folder: String?
    /** Human-readable description, or `null` if none set. */
    public var description: String?
    /** Typed properties declared on the event. */
    public var properties: [ListEventsResponseInnerPropertiesInner]
    /** `1` if this is an auto-discovered name awaiting approval (metrics on it fail until approved), `0` if approved/usable. */
    public var pending: Int
    /** ISO-8601 timestamp of creation. */
    public var createdAt: String

    public init(id: String, name: String, folder: String? = nil, description: String?, properties: [ListEventsResponseInnerPropertiesInner], pending: Int, createdAt: String) {
        self.id = id
        self.name = name
        self.folder = folder
        self.description = description
        self.properties = properties
        self.pending = pending
        self.createdAt = createdAt
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case id
        case name
        case folder
        case description
        case properties
        case pending
        case createdAt
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(id, forKey: .id)
        try container.encode(name, forKey: .name)
        try container.encodeIfPresent(folder, forKey: .folder)
        try container.encode(description, forKey: .description)
        try container.encode(properties, forKey: .properties)
        try container.encode(pending, forKey: .pending)
        try container.encode(createdAt, forKey: .createdAt)
    }
}


@available(iOS 13, tvOS 13, watchOS 6, macOS 10.15, *)
extension ListEventsResponseInner: Identifiable {}
