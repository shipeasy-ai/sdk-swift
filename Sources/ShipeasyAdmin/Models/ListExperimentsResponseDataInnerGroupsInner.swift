//
// ListExperimentsResponseDataInnerGroupsInner.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** One variant in the experiment. Weights across all groups must sum to 10000. */
public struct ListExperimentsResponseDataInnerGroupsInner: Codable, JSONEncodable {

    public static let nameRule = StringRule(minLength: 1, maxLength: 64, pattern: nil)
    public static let weightRule = NumericRule<Int>(minimum: 0, exclusiveMinimum: false, maximum: 10000, exclusiveMaximum: false, multipleOf: nil)
    /** Group label (e.g. `control`, `treatment_a`). Max 64 chars. */
    public var name: String
    /** Allocation weight in basis points (0–10000). The sum across all groups must equal exactly **10000** (100%). */
    public var weight: Int
    /** Per-group parameter values delivered to the SDK when a caller is hashed into this group. Keys must match the experiment's `params` schema. */
    public var params: [String: AnyCodable]?

    public init(name: String, weight: Int, params: [String: AnyCodable]? = nil) {
        self.name = name
        self.weight = weight
        self.params = params
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case name
        case weight
        case params
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(name, forKey: .name)
        try container.encode(weight, forKey: .weight)
        try container.encodeIfPresent(params, forKey: .params)
    }
}

