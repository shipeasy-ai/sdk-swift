//
// SetKillswitchValueRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Body for &#x60;PUT /api/admin/killswitches/{id}/value&#x60;. Sets the flat &#x60;value&#x60; on one env without touching &#x60;switches&#x60; or other envs. */
public struct SetKillswitchValueRequest: Codable, JSONEncodable {

    public var env: Env
    /** Flat boolean to publish on `env`. Publishes a new version on that env only. */
    public var value: Bool

    public init(env: Env, value: Bool) {
        self.env = env
        self.value = value
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case env
        case value
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(env, forKey: .env)
        try container.encode(value, forKey: .value)
    }
}

