//
// ErrorCode.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Stable, machine-readable error code. Mirrors the &#x60;ErrorCode&#x60; catalogue in &#x60;@shipeasy/core&#x60;. Each operation lists the subset it can return under &#x60;x-error-codes&#x60;. */
public enum ErrorCode: String, Codable, CaseIterable {
    /// Malformed request (bad JSON, missing project scope).
    case badRequest = "BAD_REQUEST"
    /// Missing or invalid admin SDK key.
    case unauthorized = "UNAUTHORIZED"
    /// Key is valid but not allowed to act on this project.
    case forbidden = "FORBIDDEN"
    /// The requested feature requires a higher plan tier (e.g. sequential testing, custom alpha, holdout, group count).
    case planRequired = "PLAN_REQUIRED"
    /// The resource does not exist or is not visible to the caller.
    case notFound = "NOT_FOUND"
    /// A resource with this name already exists in the project.
    case alreadyExists = "ALREADY_EXISTS"
    /// The requested lifecycle transition is not allowed from the current state.
    case invalidTransition = "INVALID_TRANSITION"
    /// A field that is immutable in the current state was modified (e.g. editing allocation while running).
    case immutableField = "IMMUTABLE_FIELD"
    /// The resource is a read-only built-in (e.g. a built-in gate-rule template) and cannot be modified or deleted.
    case readOnly = "READ_ONLY"
    /// The resource cannot be archived/deleted while another resource still references it.
    case referencedInUse = "REFERENCED_IN_USE"
    /// The request body failed structural (schema) validation.
    case validation = "VALIDATION"
    /// A referenced entity (universe, metric, gate, event) does not exist.
    case referencedNotFound = "REFERENCED_NOT_FOUND"
    /// Experiment group weights must sum to 10000 (basis points).
    case groupsWeightSum = "GROUPS_WEIGHT_SUM"
    /// The referenced event is still pending review and cannot back a metric yet.
    case eventPending = "EVENT_PENDING"
    /// Unexpected server error.
    case _internal = "INTERNAL"
    /// The action would exceed a plan quota (e.g. the tier&#39;s maximum experiments, metrics, or configs). Upgrade the plan or archive an existing resource.
    case planLimit = "PLAN_LIMIT"
    /// The experiment has no goal metric set, which the requested action requires.
    case experimentNoGoalMetric = "EXPERIMENT_NO_GOAL_METRIC"
    /// An archived experiment cannot be restarted.
    case experimentArchivedRestart = "EXPERIMENT_ARCHIVED_RESTART"
    /// The experiment cannot be restored from its current state.
    case experimentRestoreInvalid = "EXPERIMENT_RESTORE_INVALID"
    /// The action requires the experiment to be running.
    case experimentNotRunning = "EXPERIMENT_NOT_RUNNING"
    /// A running experiment cannot be archived — stop it first.
    case experimentRunningArchive = "EXPERIMENT_RUNNING_ARCHIVE"
    /// A field that is immutable while the experiment is running was modified.
    case experimentImmutableField = "EXPERIMENT_IMMUTABLE_FIELD"
    /// The referenced metric does not exist or is not visible to the caller.
    case metricNotFound = "METRIC_NOT_FOUND"
    /// The supplied metric id is malformed or not recognised.
    case metricUnknownId = "METRIC_UNKNOWN_ID"
}
