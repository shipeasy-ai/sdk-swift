//
// SetExperimentMetricsResponseMetricsInner.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct SetExperimentMetricsResponseMetricsInner: Codable, JSONEncodable {

    public enum Role: String, Codable, CaseIterable {
        case goal = "goal"
        case guardrail = "guardrail"
        case secondary = "secondary"
    }
    public var metricId: String
    public var role: Role
    /** The resolved per-experiment override, or `null` when this attachment inherits the metric's `default_min_effect_of_interest`. */
    public var minEffectOfInterest: Double?

    public init(metricId: String, role: Role, minEffectOfInterest: Double?) {
        self.metricId = metricId
        self.role = role
        self.minEffectOfInterest = minEffectOfInterest
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case metricId = "metric_id"
        case role
        case minEffectOfInterest = "min_effect_of_interest"
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(metricId, forKey: .metricId)
        try container.encode(role, forKey: .role)
        try container.encode(minEffectOfInterest, forKey: .minEffectOfInterest)
    }
}

