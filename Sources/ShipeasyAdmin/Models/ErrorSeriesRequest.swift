//
// ErrorSeriesRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Time window for the occurrence series. Bounds are epoch **seconds** (Analytics Engine stores the occurrence timestamp in seconds). &#x60;to&#x60; must be strictly greater than &#x60;from&#x60;. */
public struct ErrorSeriesRequest: Codable, JSONEncodable {

    public static let fromRule = NumericRule<Int>(minimum: 0, exclusiveMinimum: false, maximum: nil, exclusiveMaximum: false, multipleOf: nil)
    public static let toRule = NumericRule<Int>(minimum: 0, exclusiveMinimum: false, maximum: nil, exclusiveMaximum: false, multipleOf: nil)
    public static let bucketRule = NumericRule<Int>(minimum: 60, exclusiveMinimum: false, maximum: 86400, exclusiveMaximum: false, multipleOf: nil)
    /** Window start, epoch seconds (inclusive). */
    public var from: Int
    /** Window end, epoch seconds (exclusive). Must be greater than `from`. */
    public var to: Int
    /** Bucket width in seconds (60s–86400s/1d). Defaults to `3600` (hourly). Each returned point is floor-aligned to this width. */
    public var bucket: Int? = 3600

    public init(from: Int, to: Int, bucket: Int? = 3600) {
        self.from = from
        self.to = to
        self.bucket = bucket
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case from
        case to
        case bucket
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(from, forKey: .from)
        try container.encode(to, forKey: .to)
        try container.encodeIfPresent(bucket, forKey: .bucket)
    }
}

