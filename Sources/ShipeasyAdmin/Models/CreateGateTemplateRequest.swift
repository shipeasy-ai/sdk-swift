//
// CreateGateTemplateRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Body for &#x60;POST /api/admin/gate-templates&#x60;. Creates a per-project (customer) template. At minimum supply &#x60;name&#x60; + &#x60;rules&#x60;. */
public struct CreateGateTemplateRequest: Codable, JSONEncodable {

    public enum Category: String, Codable, CaseIterable {
        case condition = "condition"
        case rollout = "rollout"
    }
    public static let nameRule = StringRule(minLength: 1, maxLength: 140, pattern: nil)
    public static let descriptionRule = StringRule(minLength: nil, maxLength: 2000, pattern: nil)
    public static let iconKeyRule = StringRule(minLength: nil, maxLength: 64, pattern: nil)
    public static let rulesRule = ArrayRule(minItems: 1, maxItems: nil, uniqueItems: false)
    /** Human label. Unique per project. */
    public var name: String
    /** One-liner shown in pickers and matched by the list `query` filter. */
    public var description: String? = ""
    public var category: Category? = .condition
    /** Display-only icon hint. */
    public var iconKey: String?
    /** Mark the attribute as request-derived (resolved at the SDK edge). */
    public var auto: Bool? = false
    /** The rule definition captured by the template. */
    public var rules: [GateTemplateRule]

    public init(name: String, description: String? = "", category: Category? = .condition, iconKey: String? = nil, auto: Bool? = false, rules: [GateTemplateRule]) {
        self.name = name
        self.description = description
        self.category = category
        self.iconKey = iconKey
        self.auto = auto
        self.rules = rules
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case name
        case description
        case category
        case iconKey = "icon_key"
        case auto
        case rules
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(name, forKey: .name)
        try container.encodeIfPresent(description, forKey: .description)
        try container.encodeIfPresent(category, forKey: .category)
        try container.encodeIfPresent(iconKey, forKey: .iconKey)
        try container.encodeIfPresent(auto, forKey: .auto)
        try container.encode(rules, forKey: .rules)
    }
}

