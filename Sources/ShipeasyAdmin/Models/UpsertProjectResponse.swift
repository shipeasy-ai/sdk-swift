//
// UpsertProjectResponse.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Result of &#x60;POST /api/admin/projects/upsert&#x60;. */
public struct UpsertProjectResponse: Codable, JSONEncodable {

    /** Stable opaque project id. */
    public var id: String
    /** Project name (the supplied `name`, or the domain on first create). */
    public var name: String
    /** Project domain, or `null` if unset. */
    public var domain: String?
    /** Email of the account that owns the project. */
    public var ownerEmail: String
    /** `true` if this call created the project, `false` if it returned an existing one. */
    public var created: Bool

    public init(id: String, name: String, domain: String?, ownerEmail: String, created: Bool) {
        self.id = id
        self.name = name
        self.domain = domain
        self.ownerEmail = ownerEmail
        self.created = created
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case id
        case name
        case domain
        case ownerEmail = "owner_email"
        case created
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(id, forKey: .id)
        try container.encode(name, forKey: .name)
        try container.encode(domain, forKey: .domain)
        try container.encode(ownerEmail, forKey: .ownerEmail)
        try container.encode(created, forKey: .created)
    }
}


@available(iOS 13, tvOS 13, watchOS 6, macOS 10.15, *)
extension UpsertProjectResponse: Identifiable {}
