//
// QueryIrAggOneOf9NumeratorFiltersInner.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** One label filter on the source event. */
public struct QueryIrAggOneOf9NumeratorFiltersInner: Codable, JSONEncodable {

    public enum Op: String, Codable, CaseIterable {
        case equal = "="
        case notEqual = "!="
        case equalTilde = "=~"
        case exclamationTilde = "!~"
    }
    public static let labelRule = StringRule(minLength: nil, maxLength: nil, pattern: "/^[a-z_][a-z0-9_]{0,63}$/")
    public static let valueRule = StringRule(minLength: nil, maxLength: 512, pattern: nil)
    /** Event property / label identifier. */
    public var label: String
    /** Match operator (`=~`/`!~` are regex). */
    public var op: Op
    /** Quoted filter value (coerced for numeric labels). */
    public var value: String

    public init(label: String, op: Op, value: String) {
        self.label = label
        self.op = op
        self.value = value
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case label
        case op
        case value
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(label, forKey: .label)
        try container.encode(op, forKey: .op)
        try container.encode(value, forKey: .value)
    }
}

