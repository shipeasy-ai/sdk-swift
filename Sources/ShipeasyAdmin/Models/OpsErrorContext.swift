//
// OpsErrorContext.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Hydrated detail for an auto-filed &#x60;error&#x60; ticket, read from the tracked &#x60;errors&#x60; row at request time (so &#x60;count&#x60;/&#x60;lastSeenAt&#x60; reflect the latest state). */
public struct OpsErrorContext: Codable, JSONEncodable {

    /** The tracked `errors` row id (fetch full detail via `related.error`). */
    public var id: String
    /** Stable dedupe key of the tracked error. */
    public var fingerprint: String
    /** Fingerprint of the upstream issue this error descends from (set when the same error was reported at an inner boundary first), or `null` for a root issue. */
    public var causedByFingerprint: String?
    /** Error class/name, e.g. `TypeError`, or `null` when the source didn't supply one. */
    public var errorType: String?
    /** The error message. */
    public var message: String
    /** The `see()` consequence subject — the `causes_the(<subject>)` value. Grep the codebase for this to find the throw site. `null` if the error was never instrumented. */
    public var subject: String?
    /** The `see()` consequence outcome — the `.to(<outcome>)` value. `null` if uninstrumented. */
    public var outcome: String?
    /** `see()` error kind: `caught` | `uncaught` | `unhandled_rejection` | `network` | `violation`. */
    public var kind: String?
    /** Which SDK side reported it: `client` | `server`. */
    public var side: String?
    /** Published env the reporting SDK ran against (dev/staging/prod). */
    public var env: String?
    /** Folded occurrence count for this fingerprint. */
    public var count: Double
    /** ISO-8601 timestamp of the first occurrence. */
    public var firstSeenAt: String
    /** ISO-8601 timestamp of the most recent occurrence. */
    public var lastSeenAt: String
    /** Distinct, id-normalized routes this error has surfaced on (UUIDs/ids collapsed to `#`). */
    public var seenUrls: [String]
    /** The last 3 non-empty lines of the latest occurrence's stack — the innermost frames closest to where the throw bubbled out, baked in as an at-a-glance locator. `null` when the source supplied no stack. Fetch the full stack via `related.error`. */
    public var stackTail: String?
    /** @shipeasy/sdk version of the latest occurrence, or `null`. */
    public var sdkVersion: String?

    public init(id: String, fingerprint: String, causedByFingerprint: String? = nil, errorType: String? = nil, message: String, subject: String? = nil, outcome: String? = nil, kind: String? = nil, side: String? = nil, env: String? = nil, count: Double, firstSeenAt: String, lastSeenAt: String, seenUrls: [String], stackTail: String? = nil, sdkVersion: String? = nil) {
        self.id = id
        self.fingerprint = fingerprint
        self.causedByFingerprint = causedByFingerprint
        self.errorType = errorType
        self.message = message
        self.subject = subject
        self.outcome = outcome
        self.kind = kind
        self.side = side
        self.env = env
        self.count = count
        self.firstSeenAt = firstSeenAt
        self.lastSeenAt = lastSeenAt
        self.seenUrls = seenUrls
        self.stackTail = stackTail
        self.sdkVersion = sdkVersion
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case id
        case fingerprint
        case causedByFingerprint
        case errorType
        case message
        case subject
        case outcome
        case kind
        case side
        case env
        case count
        case firstSeenAt
        case lastSeenAt
        case seenUrls
        case stackTail
        case sdkVersion
    }

    public var additionalProperties: [String: AnyCodable] = [:]

    public subscript(key: String) -> AnyCodable? {
        get {
            if let value = additionalProperties[key] {
                return value
            }
            return nil
        }

        set {
            additionalProperties[key] = newValue
        }
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(id, forKey: .id)
        try container.encode(fingerprint, forKey: .fingerprint)
        try container.encodeIfPresent(causedByFingerprint, forKey: .causedByFingerprint)
        try container.encodeIfPresent(errorType, forKey: .errorType)
        try container.encode(message, forKey: .message)
        try container.encodeIfPresent(subject, forKey: .subject)
        try container.encodeIfPresent(outcome, forKey: .outcome)
        try container.encodeIfPresent(kind, forKey: .kind)
        try container.encodeIfPresent(side, forKey: .side)
        try container.encodeIfPresent(env, forKey: .env)
        try container.encode(count, forKey: .count)
        try container.encode(firstSeenAt, forKey: .firstSeenAt)
        try container.encode(lastSeenAt, forKey: .lastSeenAt)
        try container.encode(seenUrls, forKey: .seenUrls)
        try container.encodeIfPresent(stackTail, forKey: .stackTail)
        try container.encodeIfPresent(sdkVersion, forKey: .sdkVersion)
        var additionalPropertiesContainer = encoder.container(keyedBy: String.self)
        try additionalPropertiesContainer.encodeMap(additionalProperties)
    }

    // Decodable protocol methods

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        id = try container.decode(String.self, forKey: .id)
        fingerprint = try container.decode(String.self, forKey: .fingerprint)
        causedByFingerprint = try container.decodeIfPresent(String.self, forKey: .causedByFingerprint)
        errorType = try container.decodeIfPresent(String.self, forKey: .errorType)
        message = try container.decode(String.self, forKey: .message)
        subject = try container.decodeIfPresent(String.self, forKey: .subject)
        outcome = try container.decodeIfPresent(String.self, forKey: .outcome)
        kind = try container.decodeIfPresent(String.self, forKey: .kind)
        side = try container.decodeIfPresent(String.self, forKey: .side)
        env = try container.decodeIfPresent(String.self, forKey: .env)
        count = try container.decode(Double.self, forKey: .count)
        firstSeenAt = try container.decode(String.self, forKey: .firstSeenAt)
        lastSeenAt = try container.decode(String.self, forKey: .lastSeenAt)
        seenUrls = try container.decode([String].self, forKey: .seenUrls)
        stackTail = try container.decodeIfPresent(String.self, forKey: .stackTail)
        sdkVersion = try container.decodeIfPresent(String.self, forKey: .sdkVersion)
        var nonAdditionalPropertyKeys = Set<String>()
        nonAdditionalPropertyKeys.insert("id")
        nonAdditionalPropertyKeys.insert("fingerprint")
        nonAdditionalPropertyKeys.insert("causedByFingerprint")
        nonAdditionalPropertyKeys.insert("errorType")
        nonAdditionalPropertyKeys.insert("message")
        nonAdditionalPropertyKeys.insert("subject")
        nonAdditionalPropertyKeys.insert("outcome")
        nonAdditionalPropertyKeys.insert("kind")
        nonAdditionalPropertyKeys.insert("side")
        nonAdditionalPropertyKeys.insert("env")
        nonAdditionalPropertyKeys.insert("count")
        nonAdditionalPropertyKeys.insert("firstSeenAt")
        nonAdditionalPropertyKeys.insert("lastSeenAt")
        nonAdditionalPropertyKeys.insert("seenUrls")
        nonAdditionalPropertyKeys.insert("stackTail")
        nonAdditionalPropertyKeys.insert("sdkVersion")
        let additionalPropertiesContainer = try decoder.container(keyedBy: String.self)
        additionalProperties = try additionalPropertiesContainer.decodeMap(AnyCodable.self, excludedKeys: nonAdditionalPropertyKeys)
    }
}


