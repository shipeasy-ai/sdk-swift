//
// GithubConnectorDataPr.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** The pull request linked to this item via &#x60;link-pr&#x60;. */
public struct GithubConnectorDataPr: Codable, JSONEncodable {

    public enum Method: String, Codable, CaseIterable {
        case closes = "closes"
        case comment = "comment"
    }
    /** Pull request number. */
    public var number: Double
    /** HTML URL of the linked pull request. */
    public var url: String
    /** ISO-8601 timestamp the PR was linked. */
    public var linkedAt: String?
    /** Whether the PR was wired to the issue on GitHub (via a closing keyword or a comment). */
    public var connectedToIssue: Bool?
    /** How the PR was connected to the issue. */
    public var method: Method?

    public init(number: Double, url: String, linkedAt: String? = nil, connectedToIssue: Bool? = nil, method: Method? = nil) {
        self.number = number
        self.url = url
        self.linkedAt = linkedAt
        self.connectedToIssue = connectedToIssue
        self.method = method
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case number
        case url
        case linkedAt
        case connectedToIssue
        case method
    }

    public var additionalProperties: [String: AnyCodable] = [:]

    public subscript(key: String) -> AnyCodable? {
        get {
            if let value = additionalProperties[key] {
                return value
            }
            return nil
        }

        set {
            additionalProperties[key] = newValue
        }
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(number, forKey: .number)
        try container.encode(url, forKey: .url)
        try container.encodeIfPresent(linkedAt, forKey: .linkedAt)
        try container.encodeIfPresent(connectedToIssue, forKey: .connectedToIssue)
        try container.encodeIfPresent(method, forKey: .method)
        var additionalPropertiesContainer = encoder.container(keyedBy: String.self)
        try additionalPropertiesContainer.encodeMap(additionalProperties)
    }

    // Decodable protocol methods

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        number = try container.decode(Double.self, forKey: .number)
        url = try container.decode(String.self, forKey: .url)
        linkedAt = try container.decodeIfPresent(String.self, forKey: .linkedAt)
        connectedToIssue = try container.decodeIfPresent(Bool.self, forKey: .connectedToIssue)
        method = try container.decodeIfPresent(Method.self, forKey: .method)
        var nonAdditionalPropertyKeys = Set<String>()
        nonAdditionalPropertyKeys.insert("number")
        nonAdditionalPropertyKeys.insert("url")
        nonAdditionalPropertyKeys.insert("linkedAt")
        nonAdditionalPropertyKeys.insert("connectedToIssue")
        nonAdditionalPropertyKeys.insert("method")
        let additionalPropertiesContainer = try decoder.container(keyedBy: String.self)
        additionalProperties = try additionalPropertiesContainer.decodeMap(AnyCodable.self, excludedKeys: nonAdditionalPropertyKeys)
    }
}

