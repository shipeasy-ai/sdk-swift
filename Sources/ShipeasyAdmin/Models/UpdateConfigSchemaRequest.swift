//
// UpdateConfigSchemaRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Body for &#x60;PATCH /api/admin/configs/{id}/schema&#x60;. Schema-only update — replaces the config&#39;s schema and re-validates existing values. */
public struct UpdateConfigSchemaRequest: Codable, JSONEncodable {

    /** Replacement JSON Schema (draft 2020-12). Validated against every published value before it lands. */
    public var schema: [String: AnyCodable]

    public init(schema: [String: AnyCodable]) {
        self.schema = schema
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case schema
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(schema, forKey: .schema)
    }
}

