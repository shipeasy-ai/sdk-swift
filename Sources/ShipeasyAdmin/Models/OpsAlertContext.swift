//
// OpsAlertContext.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Hydrated detail for an auto-filed &#x60;alert&#x60; ticket, resolving the rule → metric → event chain at request time. */
public struct OpsAlertContext: Codable, JSONEncodable {

    public enum Source: String, Codable, CaseIterable {
        case metricRule = "metric_rule"
        case experimentSrm = "experiment_srm"
        case experimentPeek = "experiment_peek"
        case guardrail = "guardrail"
    }
    public enum Severity: String, Codable, CaseIterable {
        case danger = "danger"
        case warn = "warn"
        case info = "info"
    }
    public enum Status: String, Codable, CaseIterable {
        case active = "active"
        case resolved = "resolved"
        case dismissed = "dismissed"
    }
    /** What kind of alert transitioned to active. */
    public var source: Source
    /** Stable per-condition key; with `source` forms the ticket's `sourceRef`. */
    public var dedupeKey: String
    /** Alert severity. */
    public var severity: Severity
    /** The metric value that tripped the rule, or `null`. */
    public var observedValue: Double?
    /** Dashboard deep link to the alert, or `null`. */
    public var href: String?
    /** Current status of the underlying alert (`active` while the condition holds), or `null` if the alert row is gone. */
    public var status: Status?
    /** ISO-8601 timestamp the alert became active, or `null`. */
    public var activeSince: String?
    /** ISO-8601 timestamp the alert cleared, or `null` while still active. */
    public var resolvedAt: String?
    /** The alert rule condition that tripped (metric-rule alerts only); `null` for SRM/peek/guardrail alerts with no rule. */
    public var rule: OpsAlertRuleSummary?
    /** The metric the alert watches, hydrated with its backing event(s); `null` if not resolvable. */
    public var metric: OpsAlertMetricSummary?

    public init(source: Source, dedupeKey: String, severity: Severity, observedValue: Double? = nil, href: String? = nil, status: Status? = nil, activeSince: String? = nil, resolvedAt: String? = nil, rule: OpsAlertRuleSummary? = nil, metric: OpsAlertMetricSummary? = nil) {
        self.source = source
        self.dedupeKey = dedupeKey
        self.severity = severity
        self.observedValue = observedValue
        self.href = href
        self.status = status
        self.activeSince = activeSince
        self.resolvedAt = resolvedAt
        self.rule = rule
        self.metric = metric
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case source
        case dedupeKey
        case severity
        case observedValue
        case href
        case status
        case activeSince
        case resolvedAt
        case rule
        case metric
    }

    public var additionalProperties: [String: AnyCodable] = [:]

    public subscript(key: String) -> AnyCodable? {
        get {
            if let value = additionalProperties[key] {
                return value
            }
            return nil
        }

        set {
            additionalProperties[key] = newValue
        }
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(source, forKey: .source)
        try container.encode(dedupeKey, forKey: .dedupeKey)
        try container.encode(severity, forKey: .severity)
        try container.encodeIfPresent(observedValue, forKey: .observedValue)
        try container.encodeIfPresent(href, forKey: .href)
        try container.encodeIfPresent(status, forKey: .status)
        try container.encodeIfPresent(activeSince, forKey: .activeSince)
        try container.encodeIfPresent(resolvedAt, forKey: .resolvedAt)
        try container.encodeIfPresent(rule, forKey: .rule)
        try container.encodeIfPresent(metric, forKey: .metric)
        var additionalPropertiesContainer = encoder.container(keyedBy: String.self)
        try additionalPropertiesContainer.encodeMap(additionalProperties)
    }

    // Decodable protocol methods

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        source = try container.decode(Source.self, forKey: .source)
        dedupeKey = try container.decode(String.self, forKey: .dedupeKey)
        severity = try container.decode(Severity.self, forKey: .severity)
        observedValue = try container.decodeIfPresent(Double.self, forKey: .observedValue)
        href = try container.decodeIfPresent(String.self, forKey: .href)
        status = try container.decodeIfPresent(Status.self, forKey: .status)
        activeSince = try container.decodeIfPresent(String.self, forKey: .activeSince)
        resolvedAt = try container.decodeIfPresent(String.self, forKey: .resolvedAt)
        rule = try container.decodeIfPresent(OpsAlertRuleSummary.self, forKey: .rule)
        metric = try container.decodeIfPresent(OpsAlertMetricSummary.self, forKey: .metric)
        var nonAdditionalPropertyKeys = Set<String>()
        nonAdditionalPropertyKeys.insert("source")
        nonAdditionalPropertyKeys.insert("dedupeKey")
        nonAdditionalPropertyKeys.insert("severity")
        nonAdditionalPropertyKeys.insert("observedValue")
        nonAdditionalPropertyKeys.insert("href")
        nonAdditionalPropertyKeys.insert("status")
        nonAdditionalPropertyKeys.insert("activeSince")
        nonAdditionalPropertyKeys.insert("resolvedAt")
        nonAdditionalPropertyKeys.insert("rule")
        nonAdditionalPropertyKeys.insert("metric")
        let additionalPropertiesContainer = try decoder.container(keyedBy: String.self)
        additionalProperties = try additionalPropertiesContainer.decodeMap(AnyCodable.self, excludedKeys: nonAdditionalPropertyKeys)
    }
}

