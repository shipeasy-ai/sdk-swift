//
// GetExperimentResultsResponse.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct GetExperimentResultsResponse: Codable, JSONEncodable {

    public enum Verdict: String, Codable, CaseIterable {
        case ship = "ship"
        case hold = "hold"
        case wait = "wait"
        case invalid = "invalid"
        case draft = "draft"
    }
    public var experiment: GetExperimentResultsResponseExperiment
    public var results: [GetExperimentResultsResponseResultsInner]
    /** Server-computed decision from the goal metric + guardrails + SRM vs. the significance threshold and min runtime: `ship` (goal significant + guardrails pass), `hold` (a guardrail regressed), `wait` (inconclusive / under-powered), `invalid` (sample-ratio mismatch), `draft` (never started). */
    public var verdict: Verdict

    public init(experiment: GetExperimentResultsResponseExperiment, results: [GetExperimentResultsResponseResultsInner], verdict: Verdict) {
        self.experiment = experiment
        self.results = results
        self.verdict = verdict
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case experiment
        case results
        case verdict
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(experiment, forKey: .experiment)
        try container.encode(results, forKey: .results)
        try container.encode(verdict, forKey: .verdict)
    }
}

