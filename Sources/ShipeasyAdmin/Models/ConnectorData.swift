//
// ConnectorData.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Connector linkage for one queue item, keyed by provider. Each provider&#39;s driver deep-merges its own trace into this blob, so any subset of providers may be present. Unknown/future providers may appear as additional keys. */
public struct ConnectorData: Codable, JSONEncodable {

    public var github: GithubConnectorData?
    public var slack: SlackConnectorData?

    public init(github: GithubConnectorData? = nil, slack: SlackConnectorData? = nil) {
        self.github = github
        self.slack = slack
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case github
        case slack
    }

    public var additionalProperties: [String: AnyCodable] = [:]

    public subscript(key: String) -> AnyCodable? {
        get {
            if let value = additionalProperties[key] {
                return value
            }
            return nil
        }

        set {
            additionalProperties[key] = newValue
        }
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(github, forKey: .github)
        try container.encodeIfPresent(slack, forKey: .slack)
        var additionalPropertiesContainer = encoder.container(keyedBy: String.self)
        try additionalPropertiesContainer.encodeMap(additionalProperties)
    }

    // Decodable protocol methods

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        github = try container.decodeIfPresent(GithubConnectorData.self, forKey: .github)
        slack = try container.decodeIfPresent(SlackConnectorData.self, forKey: .slack)
        var nonAdditionalPropertyKeys = Set<String>()
        nonAdditionalPropertyKeys.insert("github")
        nonAdditionalPropertyKeys.insert("slack")
        let additionalPropertiesContainer = try decoder.container(keyedBy: String.self)
        additionalProperties = try additionalPropertiesContainer.decodeMap(AnyCodable.self, excludedKeys: nonAdditionalPropertyKeys)
    }
}

