//
// ListGatesResponseDataInnerStackInnerOneOf.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Rule-based sub-gate. When the rules match and the caller falls in the per-condition rollout bucket, evaluation short-circuits and the gate returns &#x60;true&#x60;; otherwise it falls through to the next entry. */
public struct ListGatesResponseDataInnerStackInnerOneOf: Codable, JSONEncodable {

    public enum ModelType: String, Codable, CaseIterable {
        case condition = "condition"
    }
    public enum Pass: String, Codable, CaseIterable {
        case all = "all"
        case any = "any"
    }
    public static let idRule = StringRule(minLength: 1, maxLength: nil, pattern: nil)
    public static let rolloutPctRule = NumericRule<Int>(minimum: 0, exclusiveMinimum: false, maximum: 10000, exclusiveMaximum: false, multipleOf: nil)
    /** Client-supplied stable id for the stack entry (used for React keys, audit diffs, and as the default per-condition rollout salt). */
    public var id: String
    /** Discriminator. `condition` = rule-based predicate sub-gate. */
    public var type: ModelType
    /** Human label shown in the dashboard. */
    public var name: String?
    /** Id of the project template this entry was seeded from, or `null` if hand-authored. */
    public var fromTemplate: String?
    /** Combinator across `rules`. `all` = AND (default), `any` = OR. */
    public var pass: Pass?
    /** Predicates evaluated under `pass`. Empty array = match nothing. */
    public var rules: [ListGatesResponseDataInnerRulesInner]?
    /** Per-condition rollout in basis points. After the rules match, the caller is bucketed at this %; in-bucket → gate `true`, otherwise fall through to the next entry. Absent ⇒ 10000 (100%, every matching caller passes). */
    public var rolloutPct: Int?
    /** Attribute used to hash the caller into the per-condition bucket. Default `userID`. */
    public var bucketBy: String?
    /** Per-condition hash salt. Defaults to the entry `id`, then the gate salt. */
    public var salt: String?
    public var ramp: ListGatesResponseDataInnerStackInnerOneOfRamp?
    /** If `true`, dashboard hides edit controls (e.g. the trailing public-floor entry). */
    public var locked: Bool?

    public init(id: String, type: ModelType, name: String? = nil, fromTemplate: String? = nil, pass: Pass? = nil, rules: [ListGatesResponseDataInnerRulesInner]? = nil, rolloutPct: Int? = nil, bucketBy: String? = nil, salt: String? = nil, ramp: ListGatesResponseDataInnerStackInnerOneOfRamp? = nil, locked: Bool? = nil) {
        self.id = id
        self.type = type
        self.name = name
        self.fromTemplate = fromTemplate
        self.pass = pass
        self.rules = rules
        self.rolloutPct = rolloutPct
        self.bucketBy = bucketBy
        self.salt = salt
        self.ramp = ramp
        self.locked = locked
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case id
        case type
        case name
        case fromTemplate
        case pass
        case rules
        case rolloutPct
        case bucketBy
        case salt
        case ramp
        case locked
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(id, forKey: .id)
        try container.encode(type, forKey: .type)
        try container.encodeIfPresent(name, forKey: .name)
        try container.encodeIfPresent(fromTemplate, forKey: .fromTemplate)
        try container.encodeIfPresent(pass, forKey: .pass)
        try container.encodeIfPresent(rules, forKey: .rules)
        try container.encodeIfPresent(rolloutPct, forKey: .rolloutPct)
        try container.encodeIfPresent(bucketBy, forKey: .bucketBy)
        try container.encodeIfPresent(salt, forKey: .salt)
        try container.encodeIfPresent(ramp, forKey: .ramp)
        try container.encodeIfPresent(locked, forKey: .locked)
    }
}


@available(iOS 13, tvOS 13, watchOS 6, macOS 10.15, *)
extension ListGatesResponseDataInnerStackInnerOneOf: Identifiable {}
