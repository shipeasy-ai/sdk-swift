//
// ErrorSeriesResponse.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Bucketed occurrence counts for one tracked error (by its fingerprint), plus the Analytics Engine SQL that produced them. */
public struct ErrorSeriesResponse: Codable, JSONEncodable {

    /** The Analytics Engine SQL executed to produce `rows` (echoed for transparency / debugging). */
    public var sql: String
    /** Bucketed occurrence series, ordered by `t` ascending. */
    public var rows: [ErrorSeriesResponseRowsInner]

    public init(sql: String, rows: [ErrorSeriesResponseRowsInner]) {
        self.sql = sql
        self.rows = rows
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case sql
        case rows
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(sql, forKey: .sql)
        try container.encode(rows, forKey: .rows)
    }
}

