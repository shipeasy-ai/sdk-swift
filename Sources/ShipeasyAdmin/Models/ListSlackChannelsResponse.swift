//
// ListSlackChannelsResponse.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Response for &#x60;GET /api/admin/slack/channels&#x60;. */
public struct ListSlackChannelsResponse: Codable, JSONEncodable {

    /** Whether a Slack connector is connected and authenticated. */
    public var connected: Bool
    /** The project's Slack channels (empty when no Slack is connected). */
    public var channels: [ListSlackChannelsResponseChannelsInner]

    public init(connected: Bool, channels: [ListSlackChannelsResponseChannelsInner]) {
        self.connected = connected
        self.channels = channels
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case connected
        case channels
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(connected, forKey: .connected)
        try container.encode(channels, forKey: .channels)
    }
}

