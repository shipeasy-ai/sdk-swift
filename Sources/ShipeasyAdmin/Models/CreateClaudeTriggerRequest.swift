//
// CreateClaudeTriggerRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Create a Claude Code routine trigger in one shot. Idempotent by &#x60;config.routineId&#x60; — re-creating with the same routine id updates the existing row. */
public struct CreateClaudeTriggerRequest: Codable, JSONEncodable {

    public enum Provider: String, Codable, CaseIterable {
        case claudeTrigger = "claude_trigger"
    }
    public static let nameRule = StringRule(minLength: 1, maxLength: 80, pattern: nil)
    public static let tokenRule = StringRule(minLength: 1, maxLength: nil, pattern: nil)
    /** Discriminator. Claude Code scheduled-routine trigger. */
    public var provider: Provider
    /** Human-readable connector label. */
    public var name: String? = "Claude trigger"
    /** Events that auto-fire the routine. Defaults to empty so the trigger does not auto-fire paid runs until events are subscribed. */
    public var events: [ConnectorEvent]?
    public var config: ClaudeTriggerConfig
    /** The routine's fire bearer token (secret). **Optional** — a tokenless trigger is recorded but not fireable until a token is added later. Encrypted into the credentials cipher; never persisted in `config` or returned. */
    public var token: String?
    /** Whether the trigger is active on create. */
    public var enabled: Bool? = true

    public init(provider: Provider, name: String? = "Claude trigger", events: [ConnectorEvent]? = nil, config: ClaudeTriggerConfig, token: String? = nil, enabled: Bool? = true) {
        self.provider = provider
        self.name = name
        self.events = events
        self.config = config
        self.token = token
        self.enabled = enabled
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case provider
        case name
        case events
        case config
        case token
        case enabled
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(provider, forKey: .provider)
        try container.encodeIfPresent(name, forKey: .name)
        try container.encodeIfPresent(events, forKey: .events)
        try container.encode(config, forKey: .config)
        try container.encodeIfPresent(token, forKey: .token)
        try container.encodeIfPresent(enabled, forKey: .enabled)
    }
}

