//
// CreateEventRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Body for &#x60;POST /api/admin/events&#x60;. Only &#x60;name&#x60; is required. */
public struct CreateEventRequest: Codable, JSONEncodable {

    public static let nameRule = StringRule(minLength: nil, maxLength: nil, pattern: "/^[a-zA-Z0-9_][a-zA-Z0-9_\\-.]{0,127}$/")
    public static let folderRule = StringRule(minLength: nil, maxLength: 256, pattern: "/^[a-zA-Z0-9_-]+$/")
    /** Event name. Starts with a letter, digit, or `_`; letters, digits, `_`, `-`, `.`; max 128 chars. Immutable after create — this is the handle metric queries reference. */
    public var name: String
    /** Optional folder name grouping items in the dashboard. Alphanumeric, `_` or `-` (no `/`). Part of the SDK lookup key (`<folder>/<name>`). */
    public var folder: String?
    /** Optional human-readable description of the event. */
    public var description: String?
    /** Typed properties declared on the event. Defaults to an empty list. */
    public var properties: [ListEventsResponseInnerPropertiesInner]?

    public init(name: String, folder: String? = nil, description: String? = nil, properties: [ListEventsResponseInnerPropertiesInner]? = nil) {
        self.name = name
        self.folder = folder
        self.description = description
        self.properties = properties
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case name
        case folder
        case description
        case properties
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(name, forKey: .name)
        try container.encodeIfPresent(folder, forKey: .folder)
        try container.encodeIfPresent(description, forKey: .description)
        try container.encodeIfPresent(properties, forKey: .properties)
    }
}

