//
// SlackConnectorDataMessage.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** The Slack message the driver posted for this item. */
public struct SlackConnectorDataMessage: Codable, JSONEncodable {

    /** Slack channel id the message was posted to. */
    public var channel: String
    /** Slack message timestamp (`ts`) — the message handle used for in-place `chat.update`. */
    public var ts: String
    /** ISO-8601 timestamp the message was posted. */
    public var postedAt: String?

    public init(channel: String, ts: String, postedAt: String? = nil) {
        self.channel = channel
        self.ts = ts
        self.postedAt = postedAt
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case channel
        case ts
        case postedAt
    }

    public var additionalProperties: [String: AnyCodable] = [:]

    public subscript(key: String) -> AnyCodable? {
        get {
            if let value = additionalProperties[key] {
                return value
            }
            return nil
        }

        set {
            additionalProperties[key] = newValue
        }
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(channel, forKey: .channel)
        try container.encode(ts, forKey: .ts)
        try container.encodeIfPresent(postedAt, forKey: .postedAt)
        var additionalPropertiesContainer = encoder.container(keyedBy: String.self)
        try additionalPropertiesContainer.encodeMap(additionalProperties)
    }

    // Decodable protocol methods

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        channel = try container.decode(String.self, forKey: .channel)
        ts = try container.decode(String.self, forKey: .ts)
        postedAt = try container.decodeIfPresent(String.self, forKey: .postedAt)
        var nonAdditionalPropertyKeys = Set<String>()
        nonAdditionalPropertyKeys.insert("channel")
        nonAdditionalPropertyKeys.insert("ts")
        nonAdditionalPropertyKeys.insert("postedAt")
        let additionalPropertiesContainer = try decoder.container(keyedBy: String.self)
        additionalProperties = try additionalPropertiesContainer.decodeMap(AnyCodable.self, excludedKeys: nonAdditionalPropertyKeys)
    }
}

